# ==============================================================================
# TAOBAO SCRAPER BOT - CONFIGURATION
# ==============================================================================
# Скопируйте этот файл в .env и заполните своими значениями
# cp .env.example .env
# ==============================================================================

# ==============================================================================
# TELEGRAM BOT
# ==============================================================================
# Получите токен у @BotFather в Telegram
BOT_TOKEN=your_telegram_bot_token_here

# ==============================================================================
# YANDEX GPT (для генерации описаний товаров)
# ==============================================================================
# Получите API ключ в Yandex Cloud Console
# https://yandex.cloud/ru/docs/iam/concepts/authorization/api-key
YANDEX_GPT_API_KEY=your_yandex_gpt_api_key_here
# ID каталога в Yandex Cloud
YANDEX_FOLDER_ID=your_yandex_folder_id_here
# Модель YandexGPT (yandexgpt-lite или yandexgpt)
YANDEX_GPT_MODEL=yandexgpt-lite
# Опциональный ID проекта Google Cloud (оставьте пустым, если не нужен)
GOOGLE_CLOUD_PROJECT=

# ==============================================================================
# OPENAI (альтернативный LLM провайдер)
# ==============================================================================
# Получите ключ на https://platform.openai.com/
OPENAI_API_KEY=your_openai_api_key_here
# Варианты моделей: gpt-4o-mini, gpt-4.1-mini, gpt-4o, o4-mini, gpt-5.1, gpt-5-mini, gpt-5-nano
OPENAI_MODEL=gpt-4o-mini
# Использовать Responses API для всех моделей OpenAI (True/False)
# Responses API - унифицированный интерфейс для всех моделей (gpt-4 и gpt-5) согласно документации OpenAI
# Если False, используется устаревший Chat Completions API (не рекомендуется)
OPENAI_USE_RESPONSES_API=True
# Уровень рассуждений для моделей семейства gpt-5
# Используется только для gpt-5 моделей, для остальных игнорируется
# Поддерживаемые значения зависят от модели:
#   - gpt-5, gpt-5-mini, gpt-5-nano: minimal, low, medium, high
#   - gpt-5.1, gpt-5.1-mini, gpt-5.1-nano: none, low, medium, high
#   - gpt-5.2 и выше: none, low, medium, high
# Если значение не поддерживается моделью, будет автоматически конвертировано
# Пустое значение = автоматический выбор по умолчанию для модели
OPENAI_REASONING_EFFORT=
# Если указана модель семейства gpt-5*, но Responses API отключён (в проекте используется Chat Completions),
# будет выполнен безопасный fallback на указанную модель:
OPENAI_FALLBACK_CHAT_MODEL=gpt-4o-mini

# Стратегия работы OpenAI:
# - legacy      — старый сценарий (предварительный перевод + общий промпт)
# - single_pass — экономичный сценарий (в OpenAI отправляются только нужные поля TMAPI в урезанном виде)
OPENAI_STRATEGY=single_pass
# Вариант промпта OpenAI:
# - compact_v3 (рекомендуется) — улучшенная версия compact_v2 с жёсткими правилами против китайских иероглифов и улучшенной стилистикой/филологией
# - compact_v2 — компактный промпт со строгими правилами
# - compact_v1 — более простой компактный вариант
# - shared     — использовать общий (длинный) промпт POST_GENERATION_PROMPT
OPENAI_PROMPT_VARIANT=compact_v3
# Максимум выходных токенов OpenAI при генерации JSON-поста (контроль стоимости по completion)
OPENAI_MAX_OUTPUT_TOKENS=2400
# Ограничения для single_pass (чтобы не раздувать prompt токенами)
OPENAI_SINGLE_PASS_MAX_SKUS=120
OPENAI_SINGLE_PASS_MAX_SKU_VALUES=60
OPENAI_SINGLE_PASS_MAX_PROP_VALUE_LEN=220
# Настройки цен токенов OpenAI (в USD за 1 000 000 токенов)
# Источник цен: https://openai.com/ru-RU/api/pricing/
# Если не указаны, используются стандартные цены из openai_pricing.py
# Пример для gpt-5-mini: OPENAI_PROMPT_PRICE_PER_1M=0.25, OPENAI_COMPLETION_PRICE_PER_1M=2.00
OPENAI_PROMPT_PRICE_PER_1M=
OPENAI_COMPLETION_PRICE_PER_1M=

# Отдельная компактная модель OpenAI для постобработки ГОТОВОГО текста поста.
# Рекомендуется использовать дешёвую mini-модель (например, gpt-4o-mini или gpt-4.1-mini),
# чтобы аккуратно править язык (числительные, падежи, недопереведённые слова)
# без сильного роста стоимости.
# Если оставить пустым, будет использована основная модель OPENAI_MODEL.
OPENAI_POSTPROCESS_MODEL=gpt-4o-mini

# ==============================================================================
# PROXYAPI (OpenAI-совместимый провайдер для РФ)
# ==============================================================================
# Получите ключ ProxyAPI на https://proxyapi.ru
PROXYAPI_API_KEY=your_proxyapi_api_key_here
# Базовый URL OpenAI-совместимого API. Обычно менять не нужно:
PROXYAPI_BASE_URL=https://api.proxyapi.ru/openai/v1
# Чат для уведомлений о балансе ProxyAPI (например, личный чат ответственного)
PROXYAPI_BILLING_CHAT_ID=
# Уведомлять ли о 402 (недостаточно средств) ProxyAPI
PROXYAPI_NOTIFY_402=False
# Настройки цен токенов ProxyAPI (в USD за 1 000 000 токенов)
# Если не указаны, используются стандартные цены из openai_pricing.py
PROXYAPI_PROMPT_PRICE_PER_1M=
PROXYAPI_COMPLETION_PRICE_PER_1M=

# ==============================================================================
# ВЫБОР LLM-ПРОВАЙДЕРОВ
# ==============================================================================
# Основной провайдер генерации (yandex, openai или proxyapi)
DEFAULT_LLM=yandex
# Провайдер для перевода и агрегирования цен (по умолчанию = DEFAULT_LLM)
TRANSLATE_PROVIDER=openai
# (legacy) переменная для совместимости, можно оставить пустой
TRANSLATE_LLM=
# Модель переводческого LLM (для OpenAI или YandexGPT)
TRANSLATE_MODEL=gpt-4o-mini
# Legacy-поле для OpenAI моделей (можно оставить пустым)
TRANSLATE_OPENAI_MODEL=
# Принудительно использовать старый Yandex Translate вместо LLM (True/False)
TRANSLATE_LEGACY=False

# ==============================================================================
# ПЕРЕКЛЮЧАТЕЛИ ПЛАТФОРМ (True/False)
# ==============================================================================
# Включить/выключить поддержку каждой платформы
# Если переменная не установлена или False, платформа считается выключенной
ENABLE_TAOBAO=True
ENABLE_TMALL=True
ENABLE_1688=True
ENABLE_PINDUODUO=False
ENABLE_SZWEGO=False

# ==============================================================================
# SZWEGO (API)
# ==============================================================================
# Szwego парсится через API, но требует валидные cookies + User-Agent.
# Эти данные храните в JSON-файле (не коммитить), а путь укажите ниже.
# Формат файла смотрите в `src/szwego_cookies_example.json`.
SZWEGO_BASE_URL=https://www.szwego.com
SZWEGO_COOKIES_FILE=cookies/szwego_cookies.json
SZWEGO_TIMEOUT=30
SZWEGO_TRANS_LANG=en
SZWEGO_TOKEN_EXPIRE_GRACE_SEC=60

# Мониторинг токена Szwego (алерты админу)
SZWEGO_MONITOR_ENABLED=True
SZWEGO_MONITOR_INTERVAL_SEC=3600
SZWEGO_TOKEN_EXPIRY_WARN_SEC=86400
SZWEGO_ALERT_MIN_INTERVAL_SEC=21600
# Опционально: URL товара для проверки API в фоне (если не задан — мониторинг по expires)
SZWEGO_HEALTHCHECK_URL=

# ==============================================================================
# TMAPI (для парсинга Taobao/Tmall/1688)
# ==============================================================================
# Получите токен на https://tmapi.top
TMAPI_TOKEN=your_tmapi_token_here
# Отдельный токен для 1688 (ali/item_detail*). Если не используете 1688, оставьте пустым.
TMAPI_ALI_TOKEN=
# Дополнительный токен для Pinduoduo (если используется)
TMAPI_PINDUODUO_TOKEN=
# Ограничение запросов в секунду (по умолчанию 5)
TMAPI_RATE_LIMIT=5
# Количество повторов при ошибках сети/429/5xx и базовая задержка (экспоненциальный backoff)
TMAPI_RETRY_ATTEMPTS=3
TMAPI_RETRY_BACKOFF=0.5

# ==============================================================================
# EXCHANGE RATE API (для конвертации валют, опционально)
# ==============================================================================
# Получите ключ на https://www.exchangerate-api.com
EXCHANGE_RATE_API_KEY=your_exchange_rate_api_key_here

# ==============================================================================
# ОПЦИИ
# ==============================================================================

# Конвертация валют (True/False)
CONVERT_CURRENCY=False

# Подпись под каждым постом и валюта по умолчанию
# Если DEFAULT_SIGNATURE пустая, пользователи задают свою подпись индивидуально
# Подпись может быть любой (например: "📝 Для заказа пишите @username или в комментариях 🛍️")
DEFAULT_SIGNATURE=
DEFAULT_CURRENCY=cny

# ID чата администратора для уведомлений об ошибках (необязательно)
# Чтобы узнать свой chat_id:
# 1. Напишите боту @userinfobot команду /start
# 2. Скопируйте ваш ID
# 3. Вставьте сюда (например: ADMIN_CHAT_ID=123456789)
ADMIN_CHAT_ID=

# Дополнительные админы бота (через запятую, Telegram ID)
# Эти пользователи имеют те же права, что и основной админ, включая /access и Mimi App.
# Пример: ADMIN_GROUP_BOT=111111111,222222222
ADMIN_GROUP_BOT=

# Ник пользователя, к которому можно обратиться по вопросам доступа к боту (без @)
# Используется в сообщениях при отказе доступа (белый/чёрный списки)
ACCESS_SUPPORT_USERNAME=

# Отправка дубликата сообщения в канал/группу (ID или @username)
# Форматы:
#   - Для группы: положительный ID (3018683678) или отрицательный (-3018683678)
#   - Для канала: @username или -1001234567890 (супергруппа)
# Пример: FORWARD_CHANNEL_ID=3018683678 или FORWARD_CHANNEL_ID=@my_channel
# ВАЖНО: Бот должен быть администратором группы/канала с правами на отправку сообщений
FORWARD_CHANNEL_ID=

# Отдельный чат для уведомлений об ошибках TMAPI (например, 439 - недостаточно средств)
TMAPI_BILLING_CHAT_ID=

# ============================================================================== 
# ЛИМИТЫ ЗАПРОСОВ (0 или пусто = без ограничений, время считается по МСК)
# ============================================================================== 
# Лимит запросов на одного пользователя в сутки
PER_USER_DAILY_LIMIT=
# Лимит запросов на одного пользователя в месяц
PER_USER_MONTHLY_LIMIT=
# Общий лимит запросов для всех пользователей в сутки
TOTAL_DAILY_LIMIT=
# Общий лимит запросов для всех пользователей в месяц
TOTAL_MONTHLY_LIMIT=
# Уведомлять ли про ошибку 439 (True/False)
TMAPI_NOTIFY_439=False

# PRICE_MODE=simple (по умолчанию) — старый сценарий: берём только максимальную цену и показываем одну строку "💰 Цена: X + доставка".
# PRICE_MODE=advanced — расширенный сценарий: переводим варианты (позиция + цена), группируем локально и выводим список ценовых позиций.
PRICE_MODE=simple

# ==============================================================================
# РЕЖИМЫ РАБОТЫ
# ==============================================================================
# DEBUG_MODE - показывать подробные логи в консоли (True/False).
# В debug-режиме дополнительно пишется полный лог LLM запросов/ответов в other/boot_debug.log (с ротацией ~100 МБ).
# MOCK_MODE - использовать данные из файлов вместо реальных API запросов (True/False)
#
# Рекомендуемые комбинации:
# Production:   DEBUG_MODE=False, MOCK_MODE=False (реальные запросы, без debug логов)
# Development:  DEBUG_MODE=True,  MOCK_MODE=False (реальные запросы + логи)
# Testing:      DEBUG_MODE=False, MOCK_MODE=True  (файлы, без логов)
# Mock Debug:   DEBUG_MODE=True,  MOCK_MODE=True  (файлы + логи)

DEBUG_MODE=False
MOCK_MODE=False

# Включение LLM-постобработки уже готового текста поста.
# Если ENABLE_POSTPROCESSING=True и корректно настроен OpenAI,
# после сборки поста будет выполнен дополнительный короткий LLM-проход,
# который аккуратно исправит язык (грамматику, стилистику, числительные,
# форматы размеров, недопереведённые слова) без изменения структуры блоков.
ENABLE_POSTPROCESSING=False

# Включение генерации хэштегов через отдельную LLM-модель.
# Если ENABLE_HASHTAGS=True, хэштеги будут генерироваться на основе готового поста
# (после генерации поста, но до постобработки).
# Провайдер задаётся через HASHTAGS_PROVIDER (yandex/openai/proxyapi),
# если пусто - используется DEFAULT_LLM.
# Модель задаётся через HASHTAGS_MODEL, если пусто - используется модель провайдера по умолчанию.
ENABLE_HASHTAGS=False
HASHTAGS_PROVIDER=
HASHTAGS_MODEL=

# Отключить проверку SSL (только если есть проблемы с сертификатами, НЕ рекомендуется!)
DISABLE_SSL_VERIFY=False

# ==============================================================================
# НАСТРОЙКИ PINDUODUO ВЕБ-СКРАПИНГА (опционально)
# ==============================================================================
PDD_USE_COOKIES=True
PDD_USER_AGENT=
PDD_COUNTRY_CODE=
PDD_PHONE_NUMBER=
PDD_LOGIN_MAX_ATTEMPTS=5
PDD_LOGIN_CODE_TIMEOUT_SEC=120
PDD_COOKIES_FILE=src/pdd_cookies.json

# ==============================================================================
# PLAYWRIGHT (опционально для Pinduoduo)
# ==============================================================================
PLAYWRIGHT_PROXY=
PLAYWRIGHT_SLOWMO_MS=100
PLAYWRIGHT_PAGE_TIMEOUT_MS=60000
PLAYWRIGHT_USE_MOBILE=False
PLAYWRIGHT_MOBILE_DEVICE=iPhone 12
PLAYWRIGHT_LOCALE=zh-CN
PLAYWRIGHT_TIMEZONE=Asia/Shanghai
PLAYWRIGHT_KEEP_BROWSER_OPEN=False
