# 📦 Руководство по переносу бота на новый сервер

Подробная инструкция по переносу Taobao Scraper Bot на новый сервер с сохранением всех важных данных.

---

## 📋 Содержание

1. [Подготовка к переносу](#подготовка-к-переносу)
2. [Экспорт данных со старого сервера](#экспорт-данных-со-старого-сервера)
3. [Установка на новом сервере](#установка-на-новом-сервере)
4. [Импорт данных на новый сервер](#импорт-данных-на-новый-сервер)
5. [Проверка работоспособности](#проверка-работоспособности)
6. [Чек-лист переноса](#чек-лист-переноса)

---

## 🔍 Подготовка к переносу

### Важные данные для сохранения

Перед переносом убедитесь, что у вас есть доступ к следующим файлам:

#### Критически важные файлы (обязательно):
- **`.env`** - все API токены и настройки бота
- **`data/access_control.json`** - белый и чёрный списки пользователей
- **`data/user_settings.json`** - настройки всех пользователей (подписи, валюты, курсы)
- **`data/rate_limits.json`** - лимиты запросов и статистика использования

#### Важные файлы (рекомендуется):
- **`data/access_control.backup.json`** - резервная копия списков доступа
- **`logs/bot_errors.log`** - история ошибок (для анализа проблем)

#### Опциональные файлы:
- **`src/pdd_cookies.json`** - если используется Pinduoduo через веб-скрапинг
- **`logs/*.log`** - другие логи (если нужна полная история)

---

## 📤 Экспорт данных со старого сервера

### Вариант 1: Автоматический экспорт (рекомендуется)

```bash
# 1. Подключитесь к старому серверу
ssh user@old-server.com

# 2. Перейдите в директорию бота
cd /path/to/TaoBao_Scraper

# 3. Запустите скрипт экспорта
./scripts/export_data.sh

# Скрипт создаст архив в папке backups/ с временной меткой
# 4. Скопируйте архив на локальный компьютер
# С другого терминала:
scp user@old-server.com:/path/to/TaoBao_Scraper/backups/bot_backup_*.tar.gz ./
```

### Вариант 1a: Ручной экспорт

```bash
# 1. Подключитесь к старому серверу
ssh user@old-server.com

# 2. Перейдите в директорию бота
cd /path/to/TaoBao_Scraper

# 3. Создайте архив с важными данными
tar -czf bot_backup_$(date +%Y%m%d_%H%M%S).tar.gz \
  .env \
  data/access_control.json \
  data/access_control.backup.json \
  data/user_settings.json \
  data/rate_limits.json \
  logs/bot_errors.log

# 4. Скопируйте архив на локальный компьютер
# С другого терминала:
scp user@old-server.com:/path/to/TaoBao_Scraper/bot_backup_*.tar.gz ./
```

### Вариант 2: Через команду бота `/dump_data`

Если у вас есть доступ к боту как администратор:

1. Откройте бота в Telegram
2. Отправьте команду `/dump_data`
3. Бот выведет JSON-дампы всех настроек пользователей и списков доступа
4. Скопируйте и сохраните эти данные в файлы

**Примечание:** Эта команда не экспортирует `.env` файл (по соображениям безопасности).

### Вариант 3: Прямое копирование файлов

Если у вас есть прямой доступ к файловой системе:

```bash
# Скопируйте файлы через SCP
scp user@old-server.com:/path/to/TaoBao_Scraper/.env ./
scp -r user@old-server.com:/path/to/TaoBao_Scraper/data ./
scp -r user@old-server.com:/path/to/TaoBao_Scraper/logs ./
```

---

## 🚀 Установка на новом сервере

### Шаг 1: Подготовка сервера

```bash
# Обновление системы (Ubuntu/Debian)
sudo apt update && sudo apt upgrade -y

# Установка необходимых пакетов
sudo apt install -y git python3 python3-pip python3-venv docker.io docker-compose

# Или для Docker Compose v2:
sudo apt install -y git python3 python3-pip python3-venv docker.io docker-compose-plugin
```

### Шаг 2: Клонирование репозитория

```bash
# Перейдите в рабочую директорию
cd /opt  # или другую директорию по вашему выбору

# Клонируйте репозиторий
git clone https://github.com/your-username/taobao-scraper-bot.git
cd taobao-scraper-bot

# Или если репозиторий приватный, используйте SSH:
# git clone git@github.com:your-username/taobao-scraper-bot.git
```

### Шаг 3: Создание структуры директорий

```bash
# Создайте необходимые директории
mkdir -p data logs

# Настройте права доступа (автоматически)
./scripts/setup_permissions.sh

# Или вручную (если пользователь botuser с UID 1000 существует):
# chown -R botuser:botuser data logs
# chmod -R 755 data logs
# find data -type f -name "*.json" -exec chmod 644 {} \;
```

---

## 📥 Импорт данных на новый сервер

### Шаг 1: Копирование файлов

#### Если использовали архив (автоматический импорт):

```bash
# 1. Скопируйте архив на новый сервер
scp bot_backup_YYYYMMDD_HHMMSS.tar.gz user@new-server.com:/opt/taobao-scraper-bot/

# 2. Подключитесь к новому серверу
ssh user@new-server.com

# 3. Перейдите в директорию бота
cd /opt/taobao-scraper-bot

# 4. Запустите скрипт импорта
./scripts/import_data.sh bot_backup_YYYYMMDD_HHMMSS.tar.gz

# Скрипт автоматически:
# - создаст резервную копию существующих файлов
# - распакует архив
# - установит правильные права доступа
# - проверит валидность JSON файлов
```

#### Если использовали архив (ручной импорт):

```bash
# 1. Скопируйте архив на новый сервер
scp bot_backup_YYYYMMDD_HHMMSS.tar.gz user@new-server.com:/opt/taobao-scraper-bot/

# 2. Подключитесь к новому серверу
ssh user@new-server.com

# 3. Перейдите в директорию бота
cd /opt/taobao-scraper-bot

# 4. Распакуйте архив
tar -xzf bot_backup_*.tar.gz

# 5. Установите правильные права доступа
chmod 600 .env
chmod 644 data/*.json
```

#### Если копировали файлы напрямую:

```bash
# Скопируйте файлы на новый сервер
scp .env user@new-server.com:/opt/taobao-scraper-bot/
scp -r data user@new-server.com:/opt/taobao-scraper-bot/
scp -r logs user@new-server.com:/opt/taobao-scraper-bot/
```

### Шаг 2: Проверка файлов

```bash
# Проверьте, что все файлы на месте
ls -la .env
ls -la data/
ls -la logs/

# Проверьте содержимое .env (убедитесь, что токены на месте)
# НЕ показывайте содержимое публично!
cat .env | grep -E "BOT_TOKEN|TMAPI_TOKEN|YANDEX" | head -3
```

### Шаг 3: Восстановление из дампа (если использовали /dump_data)

Если вы использовали команду `/dump_data` в боте:

```bash
# 1. Создайте файлы из скопированных данных
# data/user_settings.json
nano data/user_settings.json
# Вставьте JSON данные настроек пользователей

# data/access_control.json
nano data/access_control.json
# Вставьте JSON данные списков доступа

# 2. Проверьте JSON на валидность
python3 -m json.tool data/user_settings.json > /dev/null && echo "OK" || echo "ERROR"
python3 -m json.tool data/access_control.json > /dev/null && echo "OK" || echo "ERROR"
```

---

## ⚙️ Настройка на новом сервере

### Шаг 1: Обновление .env файла

Проверьте и при необходимости обновите следующие настройки:

```bash
# Откройте .env для редактирования
nano .env

# Проверьте следующие параметры:
# - BOT_TOKEN (должен быть тот же)
# - ADMIN_CHAT_ID (если изменился ваш Telegram ID)
# - MINI_APP_URL (если изменился домен/порт)
# - FORWARD_CHANNEL_ID (если изменился канал)
```

### Шаг 2: Установка зависимостей

#### Вариант A: Docker (рекомендуется)

```bash
# Соберите и запустите контейнер
docker-compose up -d --build

# Проверьте статус
docker-compose ps
docker-compose logs -f taobao-bot
```

#### Вариант B: Локальная установка

```bash
# Создайте виртуальное окружение
python3 -m venv .venv
source .venv/bin/activate

# Установите зависимости
pip install --upgrade pip
pip install -r requirements.txt

# Запустите бота
python main.py
```

---

## ✅ Проверка работоспособности

### Шаг 1: Проверка логов

```bash
# Docker
docker-compose logs -f taobao-bot

# Локально
tail -f logs/bot_errors.log
```

### Шаг 2: Проверка через бота

1. Откройте бота в Telegram
2. Отправьте команду `/start`
3. Проверьте, что приветственное сообщение корректное
4. Проверьте настройки: `/access` (если админ) или "ℹ️ Мои настройки"
5. Отправьте тестовую ссылку на товар

### Шаг 3: Проверка данных

```bash
# Проверьте, что данные загрузились
# Docker
docker-compose exec taobao-bot python -c "
import json
with open('data/user_settings.json', 'r') as f:
    data = json.load(f)
    print(f'Пользователей: {len(data)}')
"

# Локально
python3 -c "
import json
with open('data/user_settings.json', 'r') as f:
    data = json.load(f)
    print(f'Пользователей: {len(data)}')
"
```

---

## 📝 Чек-лист переноса

Используйте этот чек-лист, чтобы ничего не забыть:

### До переноса:
- [ ] Экспортирован `.env` файл
- [ ] Экспортирован `data/access_control.json`
- [ ] Экспортирован `data/user_settings.json`
- [ ] Экспортирован `data/rate_limits.json`
- [ ] Экспортирован `data/access_control.backup.json` (если есть)
- [ ] Экспортированы логи (если нужны)
- [ ] Создан архив или скопированы файлы на локальный компьютер

### На новом сервере:
- [ ] Установлены необходимые пакеты (git, python, docker)
- [ ] Клонирован репозиторий
- [ ] Скопированы все важные файлы
- [ ] Проверены права доступа на файлы
- [ ] Обновлён `.env` (если нужно)
- [ ] Проверена валидность JSON файлов
- [ ] Запущен бот (Docker или локально)
- [ ] Проверены логи на ошибки
- [ ] Протестирован бот через Telegram
- [ ] Проверены настройки пользователей
- [ ] Проверены списки доступа
- [ ] Проверена работа парсинга товаров

### После переноса:
- [ ] Старый сервер остановлен (если не нужен)
- [ ] Обновлены DNS/прокси (если использовались)
- [ ] Уведомлены пользователи (если были перерывы)
- [ ] Создана резервная копия на новом сервере

---

## 🔒 Безопасность

### Важные рекомендации:

1. **Никогда не коммитьте `.env` в Git!**
   ```bash
   # Убедитесь, что .env в .gitignore
   echo ".env" >> .gitignore
   ```

2. **Используйте безопасное копирование файлов:**
   ```bash
   # Используйте SCP вместо FTP
   scp -P 22 file user@server:/path/
   ```

3. **Ограничьте права доступа:**
   ```bash
   chmod 600 .env  # Только владелец может читать/писать
   chmod 644 data/*.json  # Владелец и группа могут читать
   ```

4. **Удалите временные файлы:**
   ```bash
   # После успешного переноса удалите архив с токенами
   rm bot_backup_*.tar.gz
   ```

---

## 🆘 Решение проблем

### Проблема: Бот не запускается

**Решение:**
```bash
# Проверьте логи
docker-compose logs taobao-bot
# или
tail -100 logs/bot_errors.log

# Проверьте .env файл
python3 -c "from src.core.config import settings; print('Config OK')"
```

### Проблема: Данные пользователей не загрузились

**Решение:**
```bash
# Проверьте формат JSON
python3 -m json.tool data/user_settings.json

# Проверьте права доступа
ls -la data/

# Проверьте, что файлы не пустые
wc -l data/*.json
```

### Проблема: Списки доступа не работают

**Решение:**
```bash
# Проверьте файл access_control.json
cat data/access_control.json

# Если файл повреждён, используйте backup
cp data/access_control.backup.json data/access_control.json
```

### Проблема: Ошибки при парсинге товаров

**Решение:**
```bash
# Проверьте токены в .env
grep -E "TMAPI_TOKEN|YANDEX_GPT" .env

# Проверьте подключение к API
curl -I https://api.tmapi.top
```

---

## 📞 Дополнительная помощь

Если возникли проблемы:

1. Проверьте логи: `logs/bot_errors.log`
2. Используйте команду `/dump_data` для экспорта текущих данных
3. Проверьте документацию в папке `docs/`
4. Создайте issue в репозитории с описанием проблемы

---

## 📚 Связанные документы

- [DEPLOYMENT.md](DEPLOYMENT.md) - Подробное руководство по развёртыванию
- [SERVICES_SETUP.md](SERVICES_SETUP.md) - Настройка API сервисов
- [FAQ.md](FAQ.md) - Часто задаваемые вопросы

---

**Последнее обновление:** 2024
