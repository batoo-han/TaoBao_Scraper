# План разработки административной панели

> **Статус:** В разработке | Версия: 1.0.0

---

## 🎯 Цели

Создать веб-интерфейс для администраторов с возможностью:
- Управления настройками приложения
- Управления LLM провайдерами
- Просмотра статистики использования
- Управления пользователями и доступом
- Просмотра и экспорта аудита персональных данных

---

## 🏗️ Архитектура

### Технологический стек

**Backend (API):**
- FastAPI — современный, быстрый веб-фреймворк
- SQLAlchemy (async) — уже используется в проекте
- Pydantic — валидация данных
- JWT — аутентификация админов

**Frontend:**
- HTML5 + CSS3 + JavaScript (Vanilla или Alpine.js)
- Простой и функциональный дизайн
- Адаптивная верстка

**Аутентификация:**
- JWT токены для сессий
- Проверка прав через `AdminUser` модель
- Безопасное хранение паролей (bcrypt)

---

## 📋 Функциональность

### 1. Управление настройками приложения

**Эндпоинты:**
- `GET /api/admin/settings` — получить текущие настройки
- `PUT /api/admin/settings/llm-provider` — изменить активного LLM провайдера
- `PUT /api/admin/settings/consent-text` — обновить текст согласия (ФЗ-152)

**UI:**
- Форма выбора LLM провайдера (YandexGPT, OpenAI, ProxiAPI)
- Редактор текста согласия
- Просмотр текущей конфигурации

### 2. Управление LLM провайдерами

**Эндпоинты:**
- `GET /api/admin/providers` — список доступных провайдеров
- `GET /api/admin/providers/{vendor}/config` — конфигурация провайдера
- `PUT /api/admin/providers/{vendor}/config` — обновить конфигурацию

**UI:**
- Список провайдеров с статусом (активный/неактивный)
- Формы для настройки каждого провайдера
- Тестирование подключения

### 3. Статистика использования

**Эндпоинты:**
- `GET /api/admin/stats/overview` — общая статистика
- `GET /api/admin/stats/users` — статистика по пользователям
- `GET /api/admin/stats/providers` — статистика по провайдерам
- `GET /api/admin/stats/usage` — детальная статистика использования

**UI:**
- Дашборд с графиками (Chart.js)
- Таблицы с фильтрацией и пагинацией
- Экспорт в CSV/JSON

### 4. Управление пользователями

**Эндпоинты:**
- `GET /api/admin/users` — список пользователей (с пагинацией)
- `GET /api/admin/users/{user_id}` — детали пользователя
- `PUT /api/admin/users/{user_id}` — обновить пользователя
- `DELETE /api/admin/users/{user_id}` — удалить пользователя (soft delete)
- `POST /api/admin/users/{user_id}/make-admin` — назначить админом
- `DELETE /api/admin/users/{user_id}/revoke-admin` — отозвать права админа

**UI:**
- Таблица пользователей с поиском и фильтрами
- Детальная страница пользователя
- Формы редактирования
- Управление правами админов

### 5. Аудит персональных данных

**Эндпоинты:**
- `GET /api/admin/audit` — список записей аудита (с фильтрами)
- `GET /api/admin/audit/{log_id}` — детали записи
- `GET /api/admin/audit/export` — экспорт в CSV/JSON

**UI:**
- Таблица записей аудита
- Фильтры по дате, действию, пользователю
- Экспорт данных

### 6. Аутентификация

**Эндпоинты:**
- `POST /api/admin/auth/login` — вход в админ-панель
- `POST /api/admin/auth/logout` — выход
- `GET /api/admin/auth/me` — текущий пользователь

**UI:**
- Страница входа
- Управление сессией

---

## 📁 Структура файлов

```
src/
├── admin/
│   ├── __init__.py
│   ├── api/
│   │   ├── __init__.py
│   │   ├── auth.py          # Аутентификация
│   │   ├── settings.py      # Настройки приложения
│   │   ├── providers.py     # LLM провайдеры
│   │   ├── stats.py         # Статистика
│   │   ├── users.py         # Управление пользователями
│   │   └── audit.py         # Аудит ПД
│   ├── models/
│   │   ├── __init__.py
│   │   └── schemas.py       # Pydantic схемы для API
│   ├── services/
│   │   ├── __init__.py
│   │   ├── auth_service.py  # Сервис аутентификации
│   │   └── admin_service.py # Дополнительные сервисы
│   ├── dependencies.py      # Зависимости FastAPI (auth, permissions)
│   └── main.py              # FastAPI приложение
├── admin_web/
│   ├── index.html           # Главная страница
│   ├── login.html           # Страница входа
│   ├── css/
│   │   └── admin.css        # Стили
│   ├── js/
│   │   ├── admin.js         # Основная логика
│   │   ├── api.js           # API клиент
│   │   └── charts.js        # Графики
│   └── assets/              # Изображения, иконки
```

---

## 🔒 Безопасность

### Аутентификация

1. **Пароли админов:**
   - Хранятся в БД с хешированием (bcrypt)
   - Минимальная длина: 12 символов
   - Требования: буквы, цифры, спецсимволы

2. **JWT токены:**
   - Срок жизни: 24 часа
   - Refresh токены: 7 дней
   - Хранение: httpOnly cookies

3. **Права доступа:**
   - Проверка через `AdminUser` модель
   - Разделение прав: `can_manage_keys`, `can_view_stats`, `can_manage_users`

### Защита от атак

- CSRF токены
- Rate limiting для API
- Валидация всех входных данных
- SQL injection защита (SQLAlchemy ORM)
- XSS защита (экранирование вывода)

---

## 📊 API Схемы (Pydantic)

### Аутентификация

```python
class LoginRequest(BaseModel):
    username: str
    password: str

class LoginResponse(BaseModel):
    access_token: str
    token_type: str = "bearer"
    user: AdminUserInfo
```

### Настройки

```python
class LLMProviderUpdate(BaseModel):
    vendor: Literal["yandex", "openai", "proxiapi"]
    config: Dict[str, Any]

class ConsentTextUpdate(BaseModel):
    text: str
```

### Статистика

```python
class StatsOverview(BaseModel):
    total_users: int
    total_requests: int
    total_tokens: int
    active_provider: str
    cache_hit_rate: float
```

### Пользователи

```python
class UserInfo(BaseModel):
    id: int
    telegram_id: int
    username: Optional[str]
    first_name: Optional[str]
    is_admin: bool
    created_at: datetime
    settings: UserSettingsInfo
```

---

## 🎨 UI/UX Дизайн

### Принципы

- **Простота:** Минималистичный интерфейс
- **Функциональность:** Все нужные функции доступны
- **Адаптивность:** Работает на всех устройствах
- **Скорость:** Быстрая загрузка и отклик

### Цветовая схема

- Основной: #2563eb (синий)
- Успех: #10b981 (зеленый)
- Ошибка: #ef4444 (красный)
- Предупреждение: #f59e0b (оранжевый)
- Фон: #f9fafb (светло-серый)

### Компоненты

- Навигационное меню (боковое)
- Таблицы с пагинацией
- Формы с валидацией
- Модальные окна для подтверждений
- Уведомления (toast)
- Графики и диаграммы

---

## 🚀 Этапы разработки

### Этап 1: Базовая структура (Текущий)
- [x] Создание структуры папок
- [ ] Настройка FastAPI приложения
- [ ] Базовая аутентификация
- [ ] Простая страница входа

### Этап 2: API Endpoints
- [ ] Аутентификация (login/logout)
- [ ] Настройки приложения
- [ ] LLM провайдеры
- [ ] Статистика
- [ ] Пользователи
- [ ] Аудит

### Этап 3: Frontend
- [ ] Главная страница (дашборд)
- [ ] Страница настроек
- [ ] Страница пользователей
- [ ] Страница статистики
- [ ] Страница аудита

### Этап 4: Интеграция и тестирование
- [ ] Интеграция с существующим ботом
- [ ] Тестирование всех функций
- [ ] Оптимизация производительности
- [ ] Документация

---

## 📝 Дополнительные функции (будущее)

- Экспорт данных в различных форматах
- Уведомления о важных событиях
- История изменений настроек
- Множественный выбор пользователей
- Массовые операции
- Расширенная аналитика

---

**Последнее обновление:** 2025-11-12

