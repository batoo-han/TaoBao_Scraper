# Настройка административной панели

> **Статус:** Базовая структура создана | Версия: 1.0.0

---

## 📋 Обзор

Административная панель предоставляет веб-интерфейс для управления ботом:
- Управление настройками приложения
- Управление LLM провайдерами
- Настройка распознавания текста на изображениях
- Просмотр статистики
- Управление пользователями
- Аудит персональных данных

---

## 🚀 Быстрый старт

### 1. Установка зависимостей

```bash
pip install -r requirements.txt
```

Новые зависимости для админ-панели:
- `fastapi` — веб-фреймворк
- `uvicorn` — ASGI сервер
- `python-jose[cryptography]` — JWT токены
- `passlib[bcrypt]` — хеширование паролей

### 2. Настройка .env

Добавьте в `.env`:

```env
# Admin Panel
ADMIN_JWT_SECRET=your-very-secret-key-change-in-production
ADMIN_PANEL_PORT=8004
```

**Важно:** В продакшене используйте надежный секретный ключ для JWT!

### 3. Создание первого админа

Используйте готовый скрипт:

```bash
python scripts/create_admin.py
```

Скрипт запросит:
1. **Telegram ID** пользователя (который уже взаимодействовал с ботом)
2. **Имя пользователя для входа** (например: `admin`) — это будет `admin_username`
3. **Пароль** (минимум 6 символов)

**Важно:** 
- Пользователь должен сначала отправить `/start` боту, чтобы появиться в базе данных
- Имя пользователя (`admin_username`) не зависит от Telegram username и может быть любым

### 4. Запуск админ-панели

```bash
# Вариант 1: Через скрипт
python run_admin.py

# Вариант 2: Напрямую через uvicorn
uvicorn src.admin.main:app --host 0.0.0.0 --port 8004 --reload
```

Админ-панель будет доступна по адресу: `http://localhost:8004`

### 5. Проверка API

```bash
# Проверка работоспособности
curl http://localhost:8004/api/admin/health

# Вход в админ-панель
curl -X POST http://localhost:8004/api/admin/auth/login \
  -H "Content-Type: application/json" \
  -d '{"username": "your_username", "password": "your_password"}'
```

---

## ⚙️ Runtime-конфигурация и перезапуск

### Динамическая конфигурация

- Все настройки загружаются в таблицу `runtime_settings` и применяются без перезапуска, если это допустимо.
- Настройки, требующие перезапуска (БД, порты, секреты), фиксируются в `pending_restart_config` и отображаются в UI.
- При инициализации `ConfigManager` автоматически применяет `pending_restart_config` к `runtime_settings` и очищает его.

### Перезапуск сервисов

**Через админ-панель:**
1. Нажмите кнопку «Перезапустить сервисы» в разделе «Настройки»
2. Подтвердите действие
3. Система автоматически:
   - Завершит все процессы бота и админ-панели
   - Запустит новые экземпляры
   - Проверит переподключение к серверу
   - Обновит страницу при успешном перезапуске

**Через командную строку:**
```bash
python scripts/restart_services.py
```

**Логи перезапуска:**
- `logs/restart.log` - логи скрипта перезапуска
- `logs/run_all_output.log` - вывод процесса `run_all.py`

**Устранение проблем:**
- Поиск и принудительное завершение зависших процессов: `python scripts/find_bot_processes.py --kill`
- Проверка логов: `logs/restart.log` и `logs/app.log`

### Логирование

- Логи сохраняются в `logs/app.log`
- Ротация: 5 МБ × 20 файлов
- Хранение: не более 30 дней или 100 МБ (удаляются самые старые)
- Уровни логирования: DEBUG, INFO, WARNING, ERROR, CRITICAL

---

## 📁 Структура проекта

```
src/admin/
├── __init__.py
├── main.py              # FastAPI приложение
├── dependencies.py      # Зависимости (auth, permissions)
├── api/
│   ├── __init__.py
│   ├── auth.py          # Аутентификация ✅
│   ├── settings.py      # Управление настройками ✅
│   ├── providers.py     # Управление LLM провайдерами ✅
│   ├── stats.py         # Статистика ✅
│   ├── system.py        # Перезапуск и служебные операции ✅
│   ├── users.py         # Пользователи ✅
│   └── audit.py         # Аудит ✅
├── models/
│   ├── __init__.py
│   └── schemas.py       # Pydantic-схемы ✅
└── services/
    ├── __init__.py
    ├── auth_service.py  # Сервис аутентификации ✅
    └── app_settings.py  # Работа с настройками приложения ✅
```

---

## 🔒 Безопасность

### Аутентификация

- **JWT токены:** Срок жизни 24 часа
- **Пароли:** Хешируются с помощью Argon2 (без ограничения длины)
- **Имя пользователя:** Отдельное поле `admin_username` (не зависит от Telegram username)
- **Права доступа:** Проверяются через модель `AdminUser`

### Текущая реализация (MVP)

⚠️ **Временное решение:** 
- Пароли админов хранятся в поле `notes` таблицы `admin_users`
- Имя пользователя хранится в поле `admin_username`

**TODO:** Создать отдельную таблицу `admin_passwords` для безопасного хранения паролей.

### Рекомендации для продакшена

1. Используйте надежный `ADMIN_JWT_SECRET` (минимум 32 символа)
2. Ограничьте CORS в `src/admin/main.py`
3. Используйте HTTPS
4. Настройте rate limiting
5. Реализуйте отдельную таблицу для паролей

---

## 📝 API Endpoints

Все API endpoints реализованы! См. полную документацию: [`ADMIN_PANEL_API.md`](ADMIN_PANEL_API.md)

### Основные endpoints:

**Аутентификация:**
- `POST /api/admin/auth/login` — вход ✅
- `GET /api/admin/auth/me` — текущий пользователь ✅
- `POST /api/admin/auth/logout` — выход ✅

**Настройки:**
- `GET /api/admin/settings` — получить настройки ✅
- `PUT /api/admin/settings/llm-provider` — изменить провайдер ✅
- `PUT /api/admin/settings/consent-text` — обновить текст согласия ✅

**Провайдеры:**
- `GET /api/admin/providers` — список провайдеров ✅
- `PUT /api/admin/providers/{vendor}` — обновить конфигурацию/активировать ✅

**Системные операции:**
- `POST /api/admin/system/restart` — инициировать перезапуск сервисов ✅

**Статистика:**
- `GET /api/admin/stats/overview` — общая статистика ✅
- `GET /api/admin/stats/users` — статистика по пользователям ✅
- `GET /api/admin/stats/providers` — статистика по провайдерам ✅

**Пользователи:**
- `GET /api/admin/users` — список пользователей ✅
- `GET /api/admin/users/{user_id}` — детали пользователя ✅
- `PUT /api/admin/users/{user_id}` — обновить пользователя ✅
- `POST /api/admin/users/{user_id}/make-admin` — назначить админом ✅
- `DELETE /api/admin/users/{user_id}/revoke-admin` — отозвать права ✅

**Аудит:**
- `GET /api/admin/audit` — список записей аудита ✅
- `GET /api/admin/audit/{log_id}` — детали записи ✅
- `GET /api/admin/audit/export/csv` — экспорт в CSV ✅

---

## 🎨 Веб-интерфейс

Веб-интерфейс полностью реализован! 🎉

### Структура

```
admin_web/
├── index.html          # Главная страница админ-панели
├── login.html          # Страница входа
├── css/
│   └── admin.css       # Стили админ-панели
└── js/
    ├── api.js          # API клиент
    └── admin.js        # Основная логика
```

### Функциональность

✅ **Дашборд:**
- Общая статистика (пользователи, запросы, токены)
- Активный провайдер
- Cache hit rate
- Запросы по периодам (сегодня, неделя, месяц)

✅ **Настройки:**
- **Базовые настройки:** BOT_TOKEN, TMAPI_TOKEN, валюты, подписи и т.д.
- **База данных:** настройки подключения к PostgreSQL
- **Системные:** порты, секреты, режим отладки
- **Изображения:** 
  - Включение/выключение анализа текста на изображениях
  - Настройка Yandex Vision (API ключ, модель)
  - Выбор целевого языка перевода (ru/en)
  - Настройка LLM для суммаризации (провайдер, модель, температура, токены)
  - Промпт для анализа распознанного текста
  - Каталог для сохранения визуализированных таблиц
- **LLM Промпт:** редактирование основного промпта для генерации описаний
- **Согласие на обработку ПД:** редактирование текста согласия
- **Уведомление о перезапуске:** автоматическое отображение, если требуются изменения

✅ **LLM Провайдеры:**
- Список всех провайдеров
- Статус активности
- Информация о конфигурации

✅ **Пользователи:**
- Список пользователей с пагинацией
- Поиск по username или имени
- Просмотр деталей пользователя
- Назначение/отзыв прав администратора

✅ **Платформы:**
- Включение/выключение поддержки платформ (Taobao, Pinduoduo, Szwego, 1688)
- Статистика по платформам

✅ **Логи:**
- Просмотр логов приложения в реальном времени
- Фильтрация по уровням (INFO, WARNING, ERROR)
- Потоковое обновление через SSE (Server-Sent Events)
- Скачивание логов

✅ **Документация:**
- Документация для администраторов
- Документация для разработчиков

✅ **Биллинг:**
- Информация о подписках и платежах (зарезервировано)

✅ **Статистика:**
- Статистика по пользователям
- Статистика по провайдерам

✅ **Аудит ПД:**
- Список записей с фильтрацией
- Поиск по действию, пользователю, датам
- Экспорт в CSV

### Доступ

После запуска админ-панели:

- **Веб-интерфейс:** http://localhost:8004
- **API документация:** http://localhost:8004/docs (Swagger UI)
- **ReDoc:** http://localhost:8004/redoc

### Запуск вместе с ботом

Для запуска бота и админ-панели одновременно используйте:

```bash
python run_all.py
```

Этот скрипт запускает оба сервиса в отдельных процессах и корректно обрабатывает завершение.

### Особенности

- 🔐 Автоматическая проверка авторизации
- 📱 Адаптивный дизайн (работает на мобильных)
- 🎨 Современный UI с анимациями
- 🔔 Уведомления (toast) для обратной связи
- ⚡ Быстрая загрузка данных
- 🔄 Автоматическое обновление при изменении данных
- 🔄 Умный перезапуск с проверкой статуса и автоматическим обновлением страницы
- 📊 Интерактивные графики и статистика
- 🔍 Расширенный поиск и фильтрация
- 📥 Экспорт данных (CSV)

---

## 🔄 Статус реализации

1. ✅ Базовая структура создана
2. ✅ Аутентификация реализована
3. ✅ API endpoints для настроек
4. ✅ API endpoints для статистики
5. ✅ API endpoints для пользователей
6. ✅ API endpoints для аудита
7. ✅ Веб-интерфейс (HTML/CSS/JS)
8. ✅ Система перезапуска с обратной связью
9. ✅ Настройки анализа изображений
10. ✅ Динамическая конфигурация через БД
11. ✅ Централизованное логирование
12. ✅ Управление платформами

---

**Последнее обновление:** 2025-11-13

