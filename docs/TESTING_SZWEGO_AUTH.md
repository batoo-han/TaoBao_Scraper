# Тестирование авторизации SZWEGO

## Обзор

Данный документ описывает процесс тестирования интеграции авторизации SZWEGO в бота на разных платформах.

## Тесты для Windows 11 (разработка)

### Предварительные требования

1. Установлен Python 12+ в виртуальном окружении `.venv`
2. Установлены все зависимости из `requirements.txt`
3. **Применена миграция БД:**
   ```powershell
   alembic upgrade head
   ```
   Это добавит новые поля в таблицу `szwego_auth` (cookies_encrypted, user_agent_encrypted, last_status, last_status_at).
4. Настроен `.env` с корректными значениями:
   - `ENABLE_SZWEGO=True`
   - `SZWEGO_AUTH_ENCRYPTION_KEY=...` (сгенерированный ключ)
   - `SZWEGO_PLAYWRIGHT_HEADLESS=False` (для визуального режима)
   - `DATABASE_URL=...` (подключение к PostgreSQL)
   - `BOT_TOKEN=...` (токен Telegram-бота)
   - `SZWEGO_USERNAME=...` и `SZWEGO_PASSWORD=...` (тестовые credentials)

### Запуск тестов

#### 1. Тест интеграции (хранилище, шифрование, статусы)

```powershell
cd J:\PyProject\Taobao_Scraper_main\TaoBao_Scraper
python scripts\test_szwego_bot_integration.py
```

**Ожидаемый результат:**
- Все тесты проходят успешно
- Данные корректно шифруются и расшифровываются
- Статусы обновляются в БД

#### 2. Тест авторизации через Playwright (визуальный режим)

```powershell
# Убедитесь, что SZWEGO_PLAYWRIGHT_HEADLESS=False в .env
python scripts\test_szwego_login_playwright.py
```

**Ожидаемый результат:**
- Браузер открывается в видимом режиме
- Логин и пароль вводятся корректно
- Каптча решается автоматически
- Cookies сохраняются в файл

**Что проверить визуально:**
- Браузер открывается и загружает страницу логина
- Поля логина/пароля заполняются автоматически
- Каптча появляется и решается автоматически
- После успешной авторизации cookies сохраняются

#### 3. Тест через Telegram-бота

1. Запустите бота:
   ```powershell
   python -m src.main
   ```

2. В Telegram откройте бота и выполните:
   - Откройте `⚙️ Настройки`
   - Выберите `🔐 Авторизация в SZWEGO`
   - Введите логин и пароль по запросу
   - Дождитесь результата авторизации

**Ожидаемый результат:**
- Бот корректно запрашивает логин и пароль
- После ввода данных запускается авторизация
- Пользователь получает понятное сообщение о результате
- Статус авторизации отображается корректно

**Что проверить:**
- Диалог авторизации работает корректно
- Можно отменить ввод в любой момент (отправить "Отмена")
- После успешной авторизации можно запросить товар SZWEGO
- Статус авторизации отображается в меню настроек

## Тесты для Ubuntu Server (production)

### Предварительные требования

1. Установлен Python 12+ на сервере
2. Установлены системные зависимости для Playwright:
   ```bash
   sudo apt-get update
   sudo apt-get install -y \
       libnss3 \
       libnspr4 \
       libatk1.0-0 \
       libatk-bridge2.0-0 \
       libcups2 \
       libdrm2 \
       libdbus-1-3 \
       libxkbcommon0 \
       libxcomposite1 \
       libxdamage1 \
       libxfixes3 \
       libxrandr2 \
       libgbm1 \
       libasound2
   ```

3. Установлен Playwright и браузеры:
   ```bash
   pip install playwright
   playwright install chromium
   ```

4. Настроен `.env` на сервере:
   - `SZWEGO_PLAYWRIGHT_HEADLESS=True` (обязательно для сервера без DISPLAY)
   - Все остальные настройки аналогичны Win11

### Запуск тестов на Ubuntu

#### 1. Тест интеграции (хранилище, шифрование, статусы)

```bash
cd /path/to/TaoBao_Scraper
python scripts/test_szwego_bot_integration.py
```

**Ожидаемый результат:** Аналогично Win11

#### 2. Тест авторизации через Playwright (headless режим)

```bash
# Убедитесь, что SZWEGO_PLAYWRIGHT_HEADLESS=True в .env
python scripts/test_szwego_login_playwright.py
```

**Ожидаемый результат:**
- Браузер запускается в headless-режиме (невидимо)
- Авторизация проходит успешно
- Cookies сохраняются

**Что проверить в логах:**
- Нет ошибок связанных с DISPLAY или графическим интерфейсом
- Логи показывают успешное прохождение каптчи
- Cookies файл создан и содержит валидные данные

#### 3. Тест через Telegram-бота на сервере

1. Запустите бота на сервере:
   ```bash
   python -m src.main
   ```

2. Выполните те же шаги, что и для Win11

**Ожидаемый результат:** Аналогично Win11, но без визуального браузера

### Проверка работы после деплоя

После деплоя на Ubuntu Server проверьте:

1. **Авторизация работает:**
   - Пользователь может пройти авторизацию через бота
   - Cookies сохраняются в БД (зашифрованные)
   - Статус авторизации отображается корректно

2. **Использование cookies:**
   - После успешной авторизации можно запросить товар SZWEGO
   - Cookies используются автоматически из БД
   - Если cookies протухли, пользователь получает предложение переавторизоваться

3. **Уведомления админа:**
   - Админ получает уведомления о результатах авторизации (если настроено)
   - Формат уведомлений корректный

4. **Безопасность:**
   - Данные в БД зашифрованы
   - Каждый пользователь использует только свои cookies

## Отладка проблем

### Проблема: Браузер не запускается на Ubuntu

**Решение:**
- Убедитесь, что установлены все системные зависимости
- Проверьте, что `SZWEGO_PLAYWRIGHT_HEADLESS=True`
- Проверьте логи на наличие ошибок DISPLAY

### Проблема: Каптча не решается

**Решение:**
- Проверьте логи на наличие ошибок алгоритма решения каптчи
- Убедитесь, что изображения каптчи загружаются корректно
- Проверьте селекторы каптчи в `.env`

### Проблема: Cookies не сохраняются в БД

**Решение:**
- Проверьте подключение к БД
- Убедитесь, что миграция БД применена (`alembic upgrade head`)
- Проверьте логи на наличие ошибок шифрования

### Проблема: Уведомления админа не приходят

**Решение:**
- Проверьте настройки `SZWEGO_ADMIN_NOTIFY_MODE` и `ADMIN_CHAT_ID`
- Убедитесь, что бот имеет права отправлять сообщения в указанный чат
- Проверьте логи на наличие ошибок отправки сообщений

## Чеклист перед деплоем

- [ ] Все тесты пройдены на Win11
- [ ] Миграция БД применена (`alembic upgrade head`)
- [ ] Настройки `.env` корректны для Ubuntu
- [ ] Системные зависимости Playwright установлены
- [ ] `SZWEGO_PLAYWRIGHT_HEADLESS=True` для сервера
- [ ] `SZWEGO_AUTH_ENCRYPTION_KEY` сгенерирован и сохранён
- [ ] Документация обновлена
