# Админ-панель Taobao Bot

## Обзор

Админ-панель предоставляет веб-интерфейс для управления Telegram-ботом Taobao Scraper.

### Возможности

- **Dashboard**: Сводная статистика и графики использования
- **Пользователи бота**: Управление пользователями (создание, удаление, настройки, лимиты)
- **Списки доступа**: Белый/чёрный список (whitelist/blacklist)
- **Статистика**: Детальная статистика использования и расходов
- **Настройки**: Глобальные настройки бота (только для админов)
- **Администраторы**: Управление пользователями админ-панели
- **Темы оформления**: Светлая/тёмная тема с автосохранением

## Архитектура

```
┌─────────────────────────────────────────────────────────────┐
│                     Браузер                                  │
│     (React + TypeScript + Tailwind CSS + Zustand)           │
│              Тёмная/светлая тема                            │
└─────────────────────┬───────────────────────────────────────┘
                      │ HTTP/HTTPS (REST API)
                      ▼
┌─────────────────────────────────────────────────────────────┐
│                  FastAPI Backend                             │
│         (REST API + JWT Authentication)                      │
│         Асинхронные эндпоинты + SQLAlchemy                   │
└─────────────────────┬───────────────────────────────────────┘
                      │ asyncpg (асинхронный драйвер)
                      ▼
┌─────────────────────────────────────────────────────────────┐
│                    PostgreSQL                                │
│              (Общая БД с ботом)                              │
└─────────────────────────────────────────────────────────────┘
```

## Технологический стек

### Frontend
- **React 18** + TypeScript
- **Vite** — сборка и dev-сервер
- **Tailwind CSS** — стилизация с поддержкой тёмной темы
- **React Query (TanStack)** — кэширование и синхронизация данных
- **Zustand** — управление состоянием (auth, theme)
- **React Router** — маршрутизация
- **Axios** — HTTP клиент с interceptors

### Backend
- **FastAPI** — асинхронный веб-фреймворк
- **SQLAlchemy 2.0** (asyncio) — ORM с асинхронными сессиями
- **asyncpg** — асинхронный драйвер PostgreSQL
- **Pydantic v2** — валидация данных
- **python-jose** — JWT токены
- **passlib** — хэширование паролей (PBKDF2-SHA256)

## Установка

### Требования

- Python 3.12+
- Node.js 20+
- PostgreSQL 16+
- Redis 7+ (опционально, для кэширования)

### Локальная разработка

1. **Установка зависимостей бэкенда**:

```bash
cd TaoBao_Scraper
.\.venv\Scripts\Activate.ps1   # Windows PowerShell
# или source .venv/bin/activate  # Linux/macOS

pip install -r admin/requirements.txt
```

2. **Установка зависимостей фронтенда**:

```bash
cd admin/frontend
npm install
```

3. **Настройка переменных окружения**:

Добавьте в `.env`:

```env
# === Админ-панель ===
ADMIN_PORT=8082
ADMIN_HOST=0.0.0.0

# ОБЯЗАТЕЛЬНО: Сгенерируйте уникальный секрет для продакшена!
# python -c "import secrets; print(secrets.token_urlsafe(32))"
ADMIN_JWT_SECRET=your_secret_key_here_min_32_chars

# CORS (домены фронтенда через запятую)
ADMIN_CORS_ORIGINS=http://localhost:5173,http://localhost:8082

# Режим отладки (True — показывает Swagger docs)
DEBUG=True

# Логирование
ADMIN_LOG_LEVEL=INFO
ADMIN_LOG_FILE=logs/admin.log
```

4. **Применение миграций** (если используете Alembic):

```bash
alembic upgrade head
```

5. **Запуск бэкенда** (из корня проекта):

```bash
python -m uvicorn admin.backend.main:app --host 0.0.0.0 --port 8082 --reload
```

6. **Запуск фронтенда** (в отдельном терминале):

```bash
cd admin/frontend
npm run dev
```

Фронтенд будет доступен по адресу: `http://localhost:5173`

### Docker

```bash
# Запуск только админ-панели
docker-compose up -d admin-panel

# Запуск всего стека (бот + админка + PostgreSQL + Redis)
docker-compose up -d
```

## Создание первого администратора

### Вариант 1. Скриптом (рекомендуется)

1. Убедитесь, что запущен PostgreSQL и заполнены `POSTGRES_*` в `.env`.
2. Активируйте виртуальное окружение и выполните скрипт:

```bash
cd TaoBao_Scraper
.\.venv\Scripts\Activate.ps1   # Windows PowerShell
python scripts/create_first_admin.py
```

3. В консоли введите:
   - логин администратора (`username`)
   - отображаемое имя (можно оставить пустым)
   - пароль (дважды)

После успешного выполнения будет создан пользователь в таблице `admin_users` с ролью `admin`.

### Вариант 2. Через Telegram Login Widget

1. Настройте `BOT_TOKEN` в `.env`
2. Добавьте домен в настройки бота через @BotFather
3. Укажите `ADMIN_CHAT_ID` (ваш Telegram ID)
4. Войдите через Telegram Login Widget
5. Обновите роль в БД с `user` на `admin`

## Темы оформления

Админ-панель поддерживает светлую и тёмную темы.

### Переключение темы
- Кнопка переключения в правом верхнем углу (☀️/🌙)
- Тема сохраняется в localStorage
- По умолчанию — тёмная тема

### Техническая реализация
- Tailwind CSS `darkMode: 'class'`
- Zustand store `useThemeStore` для состояния
- CSS-переменные для плавных переходов

## API Документация

После запуска бэкенда в режиме `DEBUG=True` доступна Swagger документация:

- **Swagger UI**: `http://localhost:8082/api/docs`
- **ReDoc**: `http://localhost:8082/api/redoc`
- **OpenAPI JSON**: `http://localhost:8082/api/openapi.json`

### Основные эндпоинты

#### Аутентификация `/api/auth`

| Метод | URL | Описание |
|-------|-----|----------|
| POST | `/login` | Вход по логину/паролю |
| POST | `/telegram` | Вход через Telegram Login Widget |
| POST | `/refresh` | Обновление токенов |
| POST | `/logout` | Выход |
| POST | `/change-password` | Смена пароля |
| GET | `/me` | Текущий пользователь |

#### Пользователи бота `/api/users`

| Метод | URL | Описание |
|-------|-----|----------|
| GET | `/` | Список пользователей (с пагинацией и поиском) |
| POST | `/` | Создать пользователя |
| GET | `/{id}` | Детали пользователя |
| DELETE | `/{id}` | Удалить пользователя |
| PUT | `/{id}/settings` | Обновить настройки |
| PUT | `/{id}/limits` | Обновить лимиты |
| POST | `/{id}/whitelist` | Добавить в белый список |
| DELETE | `/{id}/whitelist` | Удалить из белого списка |
| POST | `/{id}/blacklist` | Добавить в чёрный список (блокировка) |
| DELETE | `/{id}/blacklist` | Удалить из чёрного списка |

#### Статистика `/api/stats`

| Метод | URL | Описание |
|-------|-----|----------|
| GET | `/overview` | Сводная статистика |
| GET | `/requests` | История запросов (с пагинацией) |
| GET | `/costs` | Расходы по периодам |
| GET | `/platforms` | Статистика по платформам |
| GET | `/users/top` | Топ пользователей |
| GET | `/peaks` | Пики активности |

#### Настройки `/api/settings` (только админы)

| Метод | URL | Описание |
|-------|-----|----------|
| GET | `/admin` | Все настройки |
| PUT | `/admin` | Обновить настройки |
| GET | `/llm` | Настройки LLM |
| PUT | `/llm` | Обновить LLM |
| GET | `/limits` | Глобальные лимиты |
| PUT | `/limits` | Обновить лимиты |
| GET | `/flags` | Флаги функций |
| PUT | `/flags` | Обновить флаги |

#### Контроль доступа `/api/access`

| Метод | URL | Описание |
|-------|-----|----------|
| GET | `/` | Получить списки доступа |
| PUT | `/` | Обновить списки доступа |

#### Администраторы `/api/admin-users`

| Метод | URL | Описание |
|-------|-----|----------|
| GET | `/` | Список администраторов |
| POST | `/` | Создать администратора |
| GET | `/{id}` | Детали администратора |
| PUT | `/{id}` | Обновить администратора |
| DELETE | `/{id}` | Удалить администратора |

## Безопасность

### Аутентификация

- **JWT токены**: Access token (15 мин) + Refresh token (7 дней)
- **PBKDF2-SHA256**: Хэширование паролей (310 000 итераций)
- **SHA-256**: Хэширование refresh токенов для хранения в БД
- **Telegram Login Widget**: Криптографическая HMAC проверка подписи

### Защита от атак

- **SQL-инъекции**: SQLAlchemy ORM с параметризованными запросами
- **XSS**: React автоматически экранирует вывод
- **CSRF**: Токен в заголовке Authorization (не в cookies)
- **Brute Force**: Ограничение попыток входа (5 попыток, блокировка 15 мин)
- **Session Hijacking**: Привязка сессии к IP и User-Agent

### Рекомендации для продакшена

1. **HTTPS обязателен**: Используйте SSL/TLS сертификат (Let's Encrypt)
2. **Секретный ключ**: Сгенерируйте уникальный `ADMIN_JWT_SECRET`:
   ```bash
   python -c "import secrets; print(secrets.token_urlsafe(32))"
   ```
3. **CORS**: Укажите только реальный домен в `ADMIN_CORS_ORIGINS`
4. **Firewall**: Ограничьте доступ к порту 8082 (только reverse proxy)
5. **Reverse Proxy**: Используйте nginx или Caddy перед приложением
6. **Мониторинг**: Следите за логами в `logs/admin.log`
7. **Резервные копии**: Регулярно бэкапьте PostgreSQL

## Роли и права

| Функция | Admin | User |
|---------|-------|------|
| Dashboard | ✅ | ✅ |
| Список пользователей бота | ✅ | ✅ (только чтение) |
| Создание/удаление пользователей | ✅ | ❌ |
| Whitelist/Blacklist управление | ✅ | ❌ |
| Настройки бота | ✅ | ❌ |
| Полная статистика | ✅ | Ограничено |
| Управление администраторами | ✅ | ❌ |
| Смена своего пароля | ✅ | ✅ |
| Переключение темы | ✅ | ✅ |

## Структура файлов

```
admin/
├── __init__.py
├── Dockerfile              # Multi-stage Docker образ
├── requirements.txt        # Python зависимости
├── backend/
│   ├── __init__.py
│   ├── main.py            # FastAPI приложение (точка входа)
│   ├── config.py          # Pydantic Settings конфигурация
│   ├── database.py        # Асинхронное подключение к БД
│   ├── auth/              # Модуль аутентификации
│   │   ├── __init__.py
│   │   ├── jwt.py         # JWT токены, хэширование паролей
│   │   ├── telegram.py    # Telegram Login Widget валидация
│   │   └── dependencies.py # FastAPI Depends для авторизации
│   ├── models/            # Pydantic схемы (request/response)
│   │   ├── __init__.py
│   │   ├── auth.py        # Схемы аутентификации
│   │   ├── user.py        # Схемы пользователей бота
│   │   ├── stats.py       # Схемы статистики
│   │   └── settings.py    # Схемы настроек
│   ├── routers/           # API эндпоинты (роутеры)
│   │   ├── __init__.py
│   │   ├── auth.py        # /api/auth/*
│   │   ├── users.py       # /api/users/*
│   │   ├── stats.py       # /api/stats/*
│   │   ├── settings.py    # /api/settings/*
│   │   ├── admin_users.py # /api/admin-users/*
│   │   └── access.py      # /api/access/*
│   └── utils/             # Утилиты
│       ├── __init__.py
│       └── security.py    # Вспомогательные функции безопасности
└── frontend/
    ├── package.json       # npm зависимости
    ├── vite.config.ts     # Конфиг Vite (proxy на backend)
    ├── tailwind.config.js # Tailwind с darkMode: 'class'
    ├── index.html         # HTML точка входа
    └── src/
        ├── main.tsx       # React точка входа + QueryClient
        ├── App.tsx        # Роутинг приложения
        ├── index.css      # Глобальные стили + Tailwind
        ├── api/           # HTTP клиент
        │   └── client.ts  # Axios + interceptors + API методы
        ├── store/         # Zustand stores
        │   ├── auth.ts    # Состояние аутентификации
        │   └── theme.ts   # Состояние темы (light/dark)
        ├── types/         # TypeScript типы
        │   └── index.ts   # Интерфейсы для API
        ├── components/    # React компоненты
        │   ├── layouts/
        │   │   └── MainLayout.tsx  # Основной layout + переключатель темы
        │   └── ui/
        │       └── LoadingSpinner.tsx
        └── pages/         # Страницы приложения
            ├── Login.tsx       # Страница входа
            ├── Dashboard.tsx   # Главная (статистика)
            ├── Users.tsx       # Список пользователей бота
            ├── UserDetails.tsx # Детали пользователя
            ├── Statistics.tsx  # Подробная статистика
            ├── Settings.tsx    # Настройки бота
            ├── Profile.tsx     # Профиль текущего админа
            └── AdminUsers.tsx  # Управление администраторами
```

## Troubleshooting

### Ошибка подключения к БД

1. Проверьте переменные `POSTGRES_*` в `.env`
2. Убедитесь, что PostgreSQL запущен
3. Проверьте права доступа пользователя БД

### CORS ошибки

Добавьте домен фронтенда в `ADMIN_CORS_ORIGINS`:
```env
ADMIN_CORS_ORIGINS=http://localhost:5173,https://admin.yourdomain.com
```

### 401 Unauthorized

1. Проверьте `ADMIN_JWT_SECRET` (должен быть одинаковым при перезапуске)
2. Очистите localStorage в браузере (DevTools → Application → Local Storage)
3. Проверьте системное время на сервере (JWT чувствителен к времени)

### Telegram Login не работает

1. Проверьте `BOT_TOKEN` в `.env`
2. Добавьте домен в настройки бота через @BotFather → Bot Settings → Domain
3. Используйте HTTPS в продакшене (Telegram требует)

### Тёмная тема не применяется

1. Очистите localStorage (`admin-theme-storage`)
2. Перезагрузите страницу (Ctrl+F5)
3. Проверьте, что Tailwind собран с `darkMode: 'class'`

### Изменения не отображаются сразу

React Query кэширует данные. Настройки по умолчанию:
- `staleTime`: 30 секунд
- `refetchOnWindowFocus`: true

При необходимости мгновенного обновления используются:
- `invalidateQueries` после мутаций
- `refetchOnMount: 'always'` для критичных данных

## Логирование

Логи записываются в `logs/admin.log` с ротацией:
- **Максимальный размер файла**: 50 МБ (настраивается через `ADMIN_LOG_MAX_SIZE_MB`)
- **Количество резервных файлов**: 3 (настраивается через `ADMIN_LOG_BACKUP_COUNT`)
- **Уровень**: INFO по умолчанию (настраивается через `ADMIN_LOG_LEVEL`)

### Уровни логирования

- `DEBUG` — все запросы, SQL, детальная отладка
- `INFO` — входы/выходы, действия пользователей, важные события
- `WARNING` — неудачные попытки входа, подозрительная активность
- `ERROR` — ошибки, исключения

## Обновление

1. Остановите сервисы:
   ```bash
   docker-compose down
   ```

2. Примените миграции:
   ```bash
   alembic upgrade head
   ```

3. Пересоберите образы:
   ```bash
   docker-compose build admin-panel
   ```

4. Запустите:
   ```bash
   docker-compose up -d
   ```

## Переменные окружения

| Переменная | По умолчанию | Описание |
|------------|--------------|----------|
| `ADMIN_HOST` | `0.0.0.0` | Хост для прослушивания |
| `ADMIN_PORT` | `8082` | Порт сервера |
| `ADMIN_JWT_SECRET` | ⚠️ обязательно | Секрет для JWT токенов |
| `ADMIN_JWT_ACCESS_TOKEN_EXPIRE_MINUTES` | `15` | Время жизни access token |
| `ADMIN_JWT_REFRESH_TOKEN_EXPIRE_DAYS` | `7` | Время жизни refresh token |
| `ADMIN_CORS_ORIGINS` | `localhost:5173` | Разрешённые CORS origins |
| `ADMIN_MAX_LOGIN_ATTEMPTS` | `5` | Макс. попыток входа |
| `ADMIN_LOGIN_LOCKOUT_MINUTES` | `15` | Время блокировки |
| `ADMIN_MAX_SESSIONS_PER_USER` | `5` | Макс. активных сессий |
| `ADMIN_LOG_LEVEL` | `INFO` | Уровень логирования |
| `ADMIN_LOG_FILE` | `logs/admin.log` | Путь к файлу логов |
| `ADMIN_LOG_MAX_SIZE_MB` | `50` | Макс. размер лог-файла |
| `ADMIN_LOG_BACKUP_COUNT` | `3` | Кол-во резервных файлов |
| `DEBUG` | `False` | Режим отладки (Swagger docs) |
