# 🔧 Решение проблем при установке зависимостей

## Проблема: TimeoutError при установке пакетов

### Симптомы

При выполнении `pip install -r requirements.txt` возникает ошибка:
```
TimeoutError: The read operation timed out
pip._vendor.urllib3.exceptions.ReadTimeoutError: HTTPSConnectionPool(host='files.pythonhosted.org', port=443): Read timed out.
```

### Причины

1. **Медленное интернет-соединение** — загрузка метаданных пакетов занимает слишком много времени
2. **Проблемы с доступом к PyPI** — блокировка или ограничение доступа к `files.pythonhosted.org`
3. **Короткий таймаут по умолчанию** — pip использует таймаут 15 секунд, что может быть недостаточно

---

## ✅ Решения

### Решение 1: Использовать скрипт установки (рекомендуется)

**Windows:**
```powershell
.\scripts\install_dependencies.ps1
```

**Linux/Mac:**
```bash
bash scripts/install_dependencies.sh
```

Скрипт автоматически:
- увеличивает таймаут до 300 секунд (5 минут)
- повторяет попытки установки (до 3 раз)
- обновляет pip перед установкой

---

### Решение 2: Увеличить таймаут вручную

```bash
pip install -r requirements.txt --default-timeout=300 --retries=5
```

**Параметры:**
- `--default-timeout=300` — таймаут 300 секунд (5 минут)
- `--retries=5` — до 5 повторных попыток при ошибках

---

### Решение 3: Использовать зеркало PyPI

Если проблемы с доступом к официальному PyPI, используйте зеркало:

**Китайское зеркало (Tsinghua):**
```bash
pip install -r requirements.txt -i https://pypi.tuna.tsinghua.edu.cn/simple --default-timeout=300
```

**Российское зеркало (если доступно):**
```bash
pip install -r requirements.txt -i https://pypi.org/simple --default-timeout=300 --trusted-host pypi.org
```

---

### Решение 4: Установка пакетов по одному

Если массовая установка не работает, установите пакеты по одному:

```bash
pip install aiogram>=3.0.0 --default-timeout=300
pip install httpx>=0.24.0 --default-timeout=300
pip install pydantic>=2.0.0 --default-timeout=300
pip install pydantic-settings>=2.0.0 --default-timeout=300
pip install certifi --default-timeout=300
pip install Pillow>=10.0.0 --default-timeout=300
pip install playwright>=1.46.0 --default-timeout=300
```

После установки playwright необходимо установить браузеры:
```bash
playwright install chromium
```

---

### Решение 5: Настройка pip.conf (постоянное решение)

Создайте файл конфигурации pip для постоянного увеличения таймаута:

**Windows:**
```
%APPDATA%\pip\pip.ini
```

**Linux/Mac:**
```
~/.pip/pip.conf
```

**Содержимое файла:**
```ini
[global]
timeout = 300
retries = 5
```

---

### Решение 6: Проверка сетевого подключения

1. **Проверьте доступность PyPI:**
   ```bash
   ping files.pythonhosted.org
   ```

2. **Проверьте DNS:**
   ```bash
   nslookup files.pythonhosted.org
   ```

3. **Проверьте прокси-настройки:**
   Если используете прокси, настройте pip:
   ```bash
   pip install -r requirements.txt --proxy http://proxy.example.com:8080
   ```

---

## 🔍 Дополнительная диагностика

### Включить подробные логи

```bash
pip install -r requirements.txt --default-timeout=300 -v
```

Флаг `-v` (verbose) покажет подробную информацию о процессе установки.

### Очистка кэша pip

Если проблемы связаны с повреждённым кэшем:

```bash
pip cache purge
pip install -r requirements.txt --default-timeout=300 --no-cache-dir
```

---

## 📋 Чеклист решения проблемы

- [ ] попробовал скрипт установки (`install_dependencies.ps1` / `install_dependencies.sh`)
- [ ] увеличил таймаут до 300 секунд
- [ ] проверил интернет-соединение
- [ ] попробовал использовать зеркало PyPI
- [ ] установил пакеты по одному
- [ ] очистил кэш pip
- [ ] проверил настройки прокси/файрвола

---

## 💡 Профилактика

1. **Используйте скрипт установки** — он автоматически применяет оптимальные настройки
2. **Настройте pip.conf** — для постоянного увеличения таймаута
3. **Используйте стабильное интернет-соединение** — особенно при первой установке
4. **Обновляйте pip регулярно:**
   ```bash
   python -m pip install --upgrade pip
   ```

---

## 📞 Если ничего не помогло

1. Проверьте версию Python (требуется 3.11+):
   ```bash
   python --version
   ```

2. Убедитесь, что виртуальное окружение активировано:
   ```bash
   # Windows
   .venv\Scripts\activate
   
   # Linux/Mac
   source .venv/bin/activate
   ```

3. Попробуйте пересоздать виртуальное окружение:
   ```bash
   # Удалите старое
   rm -rf .venv  # Linux/Mac
   rmdir /s .venv  # Windows
   
   # Создайте новое
   python -m venv .venv
   
   # Активируйте и установите
   .venv\Scripts\activate  # Windows
   source .venv/bin/activate  # Linux/Mac
   pip install -r requirements.txt --default-timeout=300
   ```

---

## 📚 Полезные ссылки

- [документация pip](https://pip.pypa.io/en/stable/)
- [pypi зеркала](https://www.pypi-mirrors.org/)
- [настройка pip](https://pip.pypa.io/en/stable/topics/configuration/)
