# Changelog

## [2.3.0] - 2025-11-12

### 🔧 Административная панель (API)

#### Новое

- **FastAPI приложение** для админ-панели
- **Аутентификация:**
  - JWT токены для сессий
  - Хеширование паролей (bcrypt)
  - Проверка прав доступа через `AdminUser`
- **API endpoints:**
  - Аутентификация (`/api/admin/auth/*`)
  - Настройки приложения (`/api/admin/settings/*`)
  - LLM провайдеры (`/api/admin/providers/*`)
  - Статистика (`/api/admin/stats/*`)
  - Управление пользователями (`/api/admin/users/*`)
  - Аудит персональных данных (`/api/admin/audit/*`)
- **Скрипт создания админа:** `scripts/create_admin.py`
- **Документация:**
  - `docs/ADMIN_PANEL_PLAN.md` — план разработки
  - `docs/ADMIN_PANEL_SETUP.md` — настройка и запуск
  - `docs/ADMIN_PANEL_API.md` — полная документация API

#### Изменения

- Добавлены зависимости: `fastapi`, `uvicorn`, `python-jose`, `passlib`
- Добавлены настройки в `src/core/config.py`: `ADMIN_JWT_SECRET`, `ADMIN_PANEL_PORT`
- Обновлен `.env.example` с настройками админ-панели
- Интегрирован веб-интерфейс с FastAPI (статичные файлы)

---

## [2.2.0] - 2025-11-12

### 🎉 MVP завершен!

#### Новое

- **Команды бота в боковом меню:**
  - `/start` — главное меню (показывается только новым пользователям)
  - `/request` — запрос описания товара
  - `/about` — информация о сервисе
  - `/faq` — часто задаваемые вопросы
  - `/settings` — настройки пользователя
  - `/mysettings` — просмотр текущих настроек
  - `/subscription` — информация о подписках
- **Улучшенный интерфейс:**
  - Кнопка "📦 Запросить описание товара" вместо "Создать пост"
  - Кнопки "Настройки" и "Подписка" убраны в боковое меню
  - Красивое форматирование сообщений с markdown и эмодзи
- **Документация:**
  - Создано полное руководство по PostgreSQL для Ubuntu (`docs/POSTGRESQL_UBUNTU_GUIDE.md`)
  - Обновлена дорожная карта проекта (`docs/ROADMAP.md`)
  - Обновлена документация по персонализации

#### Изменения

- Обновлен интерфейс бота для более удобной навигации
- Улучшено форматирование сообщений о настройках
- Оптимизирована структура меню

---

## [2.2.0] - 2025-11-12 (предыдущая версия)

### 🎯 Мульти-провайдеры LLM и полная персонализация

#### Новое

- **Поддержка нескольких LLM провайдеров:**
  - YandexGPT (существующий)
  - OpenAI (новый)
  - ProxiAPI.ru (новый)
  - Админ выбирает активного провайдера для всех пользователей
- **Alembic миграции** для управления схемой БД
- **Полная интеграция UserSettings** в scraper и handlers
- Настройки OpenAI и ProxiAPI в конфигурации
- **Скрипты инициализации БД:**
  - `scripts/bootstrap_db.py` — комплексная инициализация
  - `scripts/init_app_settings.py` — настройка AppSettings
  - `src/db/init_db.py` — создание таблиц

#### Изменения

- `scraper.scrape_product()` теперь принимает `user_settings: UserSettings` вместо отдельных параметров
- `LLMProviderManager` поддерживает выбор провайдера из БД
- Все LLM клиенты реализуют единый интерфейс `LLMProvider`
- Обновлён `handlers.py` для передачи `user_settings` в scraper
- Обновлён `.env.example` с настройками для всех LLM провайдеров

---

## [2.1.0] - 2025-11-12

### 🎯 Персонализация и подготовка к мульти-провайдерам

#### Новое

- PostgreSQL схема для хранения пользователей, настроек и аудита (`docs/DB_SCHEMA_PLAN.md`)
- Слой сервисов (`UserSettingsService`, `AppSettingsService`, `LLMProviderManager`)
- Унифицированный интерфейс LLM-провайдеров (`api/llm/base.py`)
- Обновлённый Telegram-интерфейс с кнопками меню и настройками
  - Изменение подписи
  - Выбор валюты с запросом курса рубля
  - Меню настроек и кнопки быстрого доступа
- Кэширование LLM-ответов и регистрация статистики запросов

#### Изменения

- `scraper.scrape_product()` теперь принимает менеджер провайдеров и пользовательские настройки
- `src/api/yandex_gpt.py` реализует интерфейс `LLMProvider`
- Добавлены зависимости: SQLAlchemy (async), asyncpg, alembic
- Обновлён `.env`/`Settings` с параметрами Postgres и дефолтами приложения
- Заложена структура для админки и будущих подписок

---

## [2.0.0] - 2025-10-21

### 🎯 Крупное обновление архитектуры

#### Изменения

**Переработана система генерации постов:**

- **YandexGPT теперь возвращает структурированный JSON** вместо готового поста
  - Формат: `{title, description, characteristics, hashtags}`
  - Более надежный и предсказуемый результат
  - Легче отлаживать и модифицировать

- **Scraper формирует финальный пост** из данных LLM и TMAPI
  - Цена, ссылка, картинки берутся напрямую из API
  - Гарантированно актуальные данные
  - Единый шаблон форматирования

#### Преимущества новой архитектуры

✅ **Надежность**: Структурированный JSON легко парсить
✅ **Гибкость**: Легко изменить формат поста без изменения промпта  
✅ **Отладка**: Четкое разделение ответственности
✅ **Производительность**: LLM фокусируется только на креативном контенте
✅ **Актуальность**: Данные всегда берутся из API

#### Технические детали

**Измененные файлы:**
- `yandex_gpt.py` - новый метод `generate_post_content()` возвращает JSON
- `scraper.py` - новый метод `_build_post_text()` формирует финальный пост
- `bot_handler.py` - добавлен `parse_mode="Markdown"` для корректного отображения
- `tmapi.py` - убран лишний debug print

**Новый промпт:**
- Четкая структура ожидаемого JSON
- Адаптивные характеристики под разные типы товаров
- Релевантные хэштеги

#### Формат поста

```markdown
**Название товара**

>Краткое описание

**Характеристика 1:** значение
**Характеристика 2:** значение

**Цена:** X юаней + доставка

📝 Для заказа пишите @annabbox или в комментариях 🛍️

#хэштег1 #хэштег2

[Ссылка](url)
```

#### Миграция

Старая версия использовала `generate_post_text()`, новая - `generate_post_content()`.
Изменения обратно несовместимы, но обновление происходит автоматически.

---

## [1.0.0] - Предыдущая версия

- Базовая функциональность парсинга Taobao/Tmall
- Генерация постов через YandexGPT (полный текст)
- Интеграция с ExchangeRate-API
- Docker support

