# Changelog

## [2.4.0] - 2025-11-27

### 🧩 Mimi App для Telegram

- реализован полноценный Mini App (статическая страница + REST API `aiohttp`) с валидацией `initData`;
- добавлен `AdminSettingsService` с горячим переключением провайдеров/моделей LLM и основных флагов (`CONVERT_CURRENCY`, `TMAPI_NOTIFY_439`, `DEBUG_MODE`, `MOCK_MODE`);
- пользователи получили веб-интерфейс для обновления подписи, валюты и курса;
- клавиатура бота дополнена кнопкой `🧩 Mimi App`, URL берётся из `MINI_APP_URL`;
- новые переменные окружения и обновлённый `docker-compose` (проброс порта 8081), документация `docs/MINI_APP.md`;
- настроен общий синглтон `UserSettingsService`, чтобы изменения в Mimi App и в командном интерфейсе не конфликтовали.

## [2.3.0] - 2025-11-24

### 🚀 Добавлена поддержка OpenAI

- реализован модуль `src/api/openai_client.py` с асинхронным клиентом `AsyncOpenAI` и гарантией JSON-ответов
- добавлена фабрика `src/api/llm_provider.py` и общая константа промпта `src/api/prompts.py`
- новое переключение `DEFAULT_LLM` + переменные `OPENAI_API_KEY`, `OPENAI_MODEL` (по умолчанию `gpt-4o-mini`)
- особый режим для моделей семейства `gpt-5.1`, использующих Responses API без temperature/max_tokens — логика включается автоматически
- переработана логика цен: бот собирает все SKU, переводит названия, убирает повторяющиеся варианты и показывает список цен только когда они действительно различаются
- `Scraper` теперь работает с единым интерфейсом LLM, что упрощает добавление новых провайдеров
- обновлены `requirements.txt`, `.env`, `README.md` и вся профильная документация (`ARCHITECTURE`, `SERVICES_SETUP`, `PROJECT_SUMMARY`, `PROJECT_STRUCTURE`, `BUSINESS_RULES`, `DEVELOPMENT_PLAN`)

### ⚡ Оптимизация переводов и цен (2025‑11‑24)

- добавлен отдельный переключатель `TRANSLATE_PROVIDER` (и legacy `TRANSLATE_LLM`) + `TRANSLATE_MODEL`, чтобы можно было использовать лёгкую модель для переводов и агрегации SKU (ускорение до 30×)
- перевод и сжатие списка вариантов теперь выполняются двумя LLM-запросами: один переводит весь JSON, второй подбирает обобщённые названия по ценам
- fallback-алгоритм на Python оставлен на случай, если выбранный провайдер не умеет структурированные ответы (например, чистый Yandex Translate)
- обновлены `scraper.py`, `openai_client.py`, `.env`, `README.md`, `docs/SERVICES_SETUP.md`, `docs/PROJECT_SUMMARY.md`, `docs/BUSINESS_RULES.md`

### 🔄 Формат выдачи постов (2025‑11‑25)

- восстановлен прежний порядок отправки сообщений: первый блок — альбом из 4 фото с подписью, второй — чистый текст, последующие — фото без текста (партиями до 10 изображений)
- внедрён безопасный сплит текста поста (по знакам препинания/переносам) с учётом лимитов Telegram (2000 символов для сообщений, 1000 для подписей)
- остатки текста больше не прикладываются к фотогруппам: всё содержание уходит до начала дополнительных альбомов
- обновлены `src/bot/handlers.py`, `docs/BUSINESS_RULES.md`

### 🔐 Безопасность и DevOps

- подтверждено хранение всех ключей исключительно в `.env`
- настроена возможность плавного отката на YandexGPT (достаточно вернуть `DEFAULT_LLM=yandex`)

---

## [2.0.0] - 2025-10-21

### 🎯 Крупное обновление архитектуры

#### Изменения

**Переработана система генерации постов:**

- **yandexgpt теперь возвращает структурированный JSON** вместо готового поста
  - формат: `{title, description, characteristics, hashtags}`
  - более надежный и предсказуемый результат
  - легче отлаживать и модифицировать

- **scraper формирует финальный пост** из данных LLM и TMAPI
  - цена, ссылка, картинки берутся напрямую из API
  - гарантированно актуальные данные
  - единый шаблон форматирования

#### Преимущества новой архитектуры

✅ **Надежность**: Структурированный JSON легко парсить
✅ **Гибкость**: Легко изменить формат поста без изменения промпта  
✅ **Отладка**: Четкое разделение ответственности
✅ **Производительность**: LLM фокусируется только на креативном контенте
✅ **Актуальность**: Данные всегда берутся из API

#### Технические детали

**Измененные файлы:**
- `yandex_gpt.py` - новый метод `generate_post_content()` возвращает json
- `scraper.py` - новый метод `_build_post_text()` формирует финальный пост
- `bot_handler.py` - добавлен `parse_mode="markdown"` для корректного отображения
- `tmapi.py` - убран лишний debug print

**Новый промпт:**
- четкая структура ожидаемого JSON
- адаптивные характеристики под разные типы товаров
- релевантные хэштеги

#### Формат поста

```markdown
**Название товара**

>Краткое описание

**Характеристика 1:** значение
**Характеристика 2:** значение

**Цена:** X юаней + доставка

📝 Для заказа пишите @annabbox или в комментариях 🛍️

#хэштег1 #хэштег2

[Ссылка](url)
```

#### Миграция

Старая версия использовала `generate_post_text()`, новая - `generate_post_content()`.
Изменения обратно несовместимы, но обновление происходит автоматически.

---

## [1.0.0] - Предыдущая версия

- базовая функциональность парсинга Taobao/Tmall
- генерация постов через YandexGPT (полный текст)
- интеграция с ExchangeRate-API
- docker support
