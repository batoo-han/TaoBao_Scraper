# Авторизация SZWEGO

## Архитектура

Сценарий авторизации построен вокруг трёх компонентов:

1. **Telegram-бот** — собирает логин/пароль и запускает авторизацию.
2. **Playwright** — открывает страницу входа, выполняет логин и решает капчу.
3. **Хранилище** — сохраняет cookies + user-agent в JSON и шифрует логин/пароль в БД.

Основные файлы:
- `src/bot/handlers.py` — диалог авторизации в настройках.
- `src/services/szwego_auth.py` — бизнес-логика входа и сохранения cookies.
- `src/services/slider_captcha_solver.py` — локальное распознавание слайдер-капчи.
- `src/utils/crypto.py` — шифрование логина/пароля.

## Бизнес-логика

1. Пользователь открывает `⚙️ Настройки` и выбирает `Авторизация в SZWEGO`.
2. Бот запрашивает логин и пароль.
3. Сервис авторизации запускает Playwright:
   - заполняет логин/пароль;
   - обнаруживает капчу и решает её локально;
   - сохраняет cookies + user-agent.
4. После успешной авторизации cookies используются для API-запросов Szwego.

## План реализации

1. Добавить кнопку авторизации в меню настроек бота.
2. Реализовать FSM-диалог для ввода логина/пароля.
3. Реализовать Playwright-авторизацию и локальный решатель капчи.
4. Добавить безопасное хранение данных в БД и JSON cookies.
5. Обновить документацию и примеры `.env`.

## Диалог авторизации в боте

Процесс авторизации выглядит так:

1. Пользователь открывает `⚙️ Настройки` и выбирает `🔐 Авторизация в SZWEGO`.
2. Бот показывает краткое объяснение безопасности данных (логин/пароль хранятся только в зашифрованном виде).
3. Пользователь вводит логин (или отправляет «Отмена» для выхода).
4. Пользователь вводит пароль (или отправляет «Отмена» для выхода).
5. Бот сохраняет данные в зашифрованном виде и запускает фоновую авторизацию.
6. После завершения авторизации пользователь получает результат:
   - ✅ «Авторизация в SZWEGO успешна» — можно использовать SZWEGO.
   - ❌ Сообщение об ошибке с понятным описанием (например, «Логин или пароль не подходят»).

Пользователь может в любой момент посмотреть статус авторизации через пункт `📊 Статус авторизации SZWEGO` в меню настроек.

## Хранение данных

### Структура в БД

Модель `SzwegoAuth` хранит:

- `login_enc` / `password_enc` — зашифрованные логин и пароль (используется `cryptography.fernet`).
- `cookies_encrypted` — зашифрованный JSON с cookies (новый формат, предпочтительно).
- `user_agent_encrypted` — зашифрованный User-Agent (новый формат, предпочтительно).
- `cookies_file` / `user_agent` — legacy-поля для обратной совместимости (путь к файлу и plaintext UA).
- `last_status` — последний статус авторизации (`success` / `invalid_credentials` / `captcha_failed` / `service_unavailable` / `unknown_error`).
- `last_status_at` — время последнего обновления статуса.

### Безопасность

- Все чувствительные данные (логин, пароль, cookies, UA) хранятся **только в зашифрованном виде**.
- Ключ шифрования задаётся через `SZWEGO_AUTH_ENCRYPTION_KEY` в `.env`.
- Даже обслуживающий персонал бота не может прочитать эти данные без ключа.
- Каждый пользователь работает только со своей учётной записью SZWEGO (данные не разделяются между пользователями).

### Использование cookies/UA

При запросе товара SZWEGO:

1. Система пытается получить cookies и UA из БД через `get_user_session(user_id)`.
2. Если данные есть — они передаются в `SzwegoApiClient` напрямую (без чтения файлов).
3. Если данных нет — используется legacy-режим (глобальный файл `SZWEGO_COOKIES_FILE`).

## Админские уведомления

Администратор может настроить уведомления о результатах авторизации SZWEGO через настройки в `.env`:

- `SZWEGO_ADMIN_NOTIFY_MODE` — режим уведомлений:
  - `none` — не отправлять уведомления.
  - `all` — отправлять все (успехи и ошибки).
  - `errors` — только неудачные попытки (по умолчанию).
- Уведомления отправляются в `ADMIN_CHAT_ID` (если задан).

Формат уведомлений:

- **Успех**: краткая информация (user_id, время, признак обновления cookies).
- **Ошибка**: тип ошибки, user_id, время, краткое описание (до 500 символов).

## Руководство пользователя

1. Включите Szwego в `.env`:
   - `ENABLE_SZWEGO=True`
2. Сгенерируйте ключ шифрования:
   - `python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"`
3. Добавьте ключ в `.env`:
   - `SZWEGO_AUTH_ENCRYPTION_KEY=...`
4. (Опционально) Настройте пул User-Agent:
   - `SZWEGO_UA_POOL="Mozilla/5.0 ... Chrome/120...,Mozilla/5.0 ... Firefox/122..."`
   - Если не задано, используются дефолтные значения из конфига
5. (Опционально) Настройте режим браузера:
   - `SZWEGO_PLAYWRIGHT_HEADLESS=True` (по умолчанию, для Ubuntu Server).
   - `SZWEGO_PLAYWRIGHT_HEADLESS=False` (для отладки на Windows 11).
6. Откройте бот, перейдите в `⚙️ Настройки`.
7. Выберите `🔐 Авторизация в SZWEGO`.
8. Введите логин и пароль (или «Отмена» для выхода).
9. Дождитесь результата авторизации или проверьте статус позже через `📊 Статус авторизации SZWEGO`.

Если капча не решается:
- проверьте CSS-селекторы капчи в `.env`;
- если капча внутри iframe — укажите `SZWEGO_CAPTCHA_FRAME_SELECTOR`;
- повторите авторизацию позднее (капча может меняться).

Признаком успешного входа считаются cookies, указанные в `SZWEGO_AUTH_REQUIRED_COOKIES`
(по умолчанию `token,JSESSIONID`).

## Тестовый скрипт

Можно проверить авторизацию без сохранения данных в БД:

1. Установите переменные окружения:
   - `SZWEGO_USERNAME`
   - `SZWEGO_PASSWORD`
2. Запустите:
   - `python scripts/test_szwego_login_playwright.py`

Cookies будут сохранены в папку `SZWEGO_AUTH_COOKIES_DIR`.

## Тестирование

Подробное руководство по тестированию авторизации SZWEGO на Windows 11 и Ubuntu Server см. в [`docs/TESTING_SZWEGO_AUTH.md`](TESTING_SZWEGO_AUTH.md).

### Быстрый старт для Win11

1. Примените миграцию БД:
   ```powershell
   alembic upgrade head
   ```

2. Запустите тест интеграции:
   ```powershell
   python scripts\test_szwego_bot_integration.py
   ```

3. Протестируйте авторизацию через бота (см. раздел "Диалог авторизации в боте" выше).
