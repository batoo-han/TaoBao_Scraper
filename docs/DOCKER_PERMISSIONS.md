# 🔒 Настройка прав доступа для Docker

Руководство по решению проблем с правами доступа при работе с Docker контейнером.

---

## 🎯 Проблема

Контейнер запускается от пользователя `botuser` (UID 1000), но папки `data/` и `logs/` монтируются из хоста и могут принадлежать другому пользователю, что приводит к ошибке:

```
PermissionError: [Errno 13] Permission denied: '/app/logs/bot_errors.log'
```

---

## ✅ Решение

### Способ 1: Автоматическая настройка (рекомендуется)

```bash
# Запустите скрипт настройки прав
./scripts/setup_permissions.sh
```

Скрипт автоматически:
- Создаст папки `data/` и `logs/` если их нет
- Найдёт или создаст пользователя с UID 1000
- Установит правильного владельца и права доступа

### Способ 2: Ручная настройка

#### Вариант A: Пользователь с UID 1000 существует

```bash
# Создайте папки
mkdir -p data logs

# Установите владельца (UID 1000 = botuser в контейнере)
chown -R 1000:1000 data logs

# Установите права доступа
chmod -R 755 data logs
find data -type f -name "*.json" -exec chmod 644 {} \;
```

#### Вариант B: Использовать текущего пользователя

```bash
# Создайте папки
mkdir -p data logs

# Установите владельца на текущего пользователя
chown -R $(id -u):$(id -g) data logs

# Установите права доступа (775 = запись для группы)
chmod -R 775 data logs
find data -type f -name "*.json" -exec chmod 664 {} \;
```

**Примечание:** Для этого варианта нужно изменить Dockerfile, чтобы использовать UID текущего пользователя вместо 1000.

#### Вариант C: Изменить UID в Dockerfile

Если на вашем сервере нет пользователя с UID 1000, измените Dockerfile:

```dockerfile
# Вместо:
RUN useradd -m -u 1000 botuser

# Используйте UID вашего пользователя:
RUN useradd -m -u $(id -u) botuser
```

Или передайте UID через build args:

```dockerfile
ARG USER_UID=1000
RUN useradd -m -u ${USER_UID} botuser
```

Сборка:
```bash
docker-compose build --build-arg USER_UID=$(id -u)
```

#### Вариант D: Временное решение (не рекомендуется)

```bash
# Открыть доступ для всех (небезопасно!)
chmod -R 777 data logs
```

**⚠️ ВНИМАНИЕ:** Этот вариант небезопасен и не рекомендуется для production!

---

## 🔍 Проверка прав доступа

### Проверка на хосте

```bash
# Проверьте владельца и права
ls -ld data logs

# Должно быть примерно так:
# drwxr-xr-x 2 botuser botuser 4096 Jan  1 12:00 data
# drwxr-xr-x 2 botuser botuser 4096 Jan  1 12:00 logs
```

### Проверка в контейнере

```bash
# Проверьте UID пользователя в контейнере
docker compose exec taobao-bot id

# Должно показать: uid=1000(botuser) gid=1000(botuser)

# Проверьте права на папки
docker compose exec taobao-bot ls -ld /app/data /app/logs

# Проверьте возможность записи
docker compose exec taobao-bot touch /app/logs/test.txt && \
docker compose exec taobao-bot rm /app/logs/test.txt && \
echo "✓ Запись работает"
```

---

## 🛠️ Решение проблем

### Проблема: "Permission denied" после настройки прав

**Решение:**

1. Убедитесь, что папки существуют:
   ```bash
   mkdir -p data logs
   ```

2. Проверьте владельца:
   ```bash
   ls -ld data logs
   ```

3. Если владелец неверный, исправьте:
   ```bash
   sudo chown -R 1000:1000 data logs
   ```

4. Перезапустите контейнер:
   ```bash
   docker compose restart taobao-bot
   ```

### Проблема: Пользователь с UID 1000 не существует

**Решение:**

1. Создайте пользователя:
   ```bash
   sudo useradd -m -u 1000 botuser
   ```

2. Или используйте существующего пользователя:
   ```bash
   # Найдите пользователя с нужным UID
   getent passwd 1000
   
   # Если пользователь есть, используйте его имя
   sudo chown -R $(getent passwd 1000 | cut -d: -f1):$(getent passwd 1000 | cut -d: -f1) data logs
   ```

### Проблема: Файлы создаются от root

**Решение:**

Это происходит если контейнер запускался от root. Исправьте:

```bash
# Остановите контейнер
docker compose down

# Исправьте права
sudo chown -R 1000:1000 data logs

# Запустите снова
docker compose up -d
```

---

## 📋 Рекомендуемые права

### Папки:
- `data/` и `logs/`: `755` (rwxr-xr-x)
  - Владелец может читать, писать, выполнять
  - Группа и остальные могут читать и выполнять

### Файлы:
- JSON файлы в `data/`: `644` (rw-r--r--)
  - Владелец может читать и писать
  - Группа и остальные могут только читать

- Логи: `644` (rw-r--r--) или `664` (rw-rw-r--)
  - Зависит от того, нужен ли групповой доступ

---

## 🔐 Безопасность

### Рекомендации:

1. **Используйте минимальные необходимые права** (755 для папок, 644 для файлов)
2. **Не используйте 777** (полный доступ для всех)
3. **Проверяйте владельца** перед запуском контейнера
4. **Используйте non-root пользователя** в контейнере (уже настроено)

### Проверка безопасности:

```bash
# Проверьте, что нет файлов с правами 777
find data logs -perm 777

# Проверьте, что .env защищён
ls -l .env
# Должно быть: -rw------- (600)

# Проверьте, что нет файлов от root
find data logs -user root
```

---

## 📚 Связанные документы

- [DEPLOYMENT.md](DEPLOYMENT.md) - Полное руководство по развёртыванию
- [MIGRATION_GUIDE.md](MIGRATION_GUIDE.md) - Перенос на новый сервер

---

**Последнее обновление:** 2024

