# Развёртывание авторизации SZWEGO

## Быстрый старт

### 1. Применение миграции БД

**Важно:** Перед использованием авторизации SZWEGO необходимо применить миграцию БД.

```bash
# Windows PowerShell
cd J:\PyProject\Taobao_Scraper_main\TaoBao_Scraper
alembic upgrade head

# Ubuntu/Linux
cd /path/to/TaoBao_Scraper
alembic upgrade head
```

Миграция добавит следующие поля в таблицу `szwego_auth`:
- `cookies_encrypted` — зашифрованные cookies (JSON)
- `user_agent_encrypted` — зашифрованный User-Agent
- `last_status` — последний статус авторизации
- `last_status_at` — время последнего обновления статуса

### 2. Настройка .env

Убедитесь, что в `.env` заданы следующие параметры:

```env
# Включить платформу SZWEGO
ENABLE_SZWEGO=True

# Ключ шифрования (сгенерировать командой ниже)
SZWEGO_AUTH_ENCRYPTION_KEY=ваш_ключ_здесь

# Режим браузера (True для Ubuntu Server, False для Win11 при отладке)
SZWEGO_PLAYWRIGHT_HEADLESS=True

# Пул User-Agent (опционально, разделены запятыми)
# Если не задано, используются дефолтные значения из конфига
SZWEGO_UA_POOL="Mozilla/5.0 ... Chrome/120...,Mozilla/5.0 ... Firefox/122..."

# Уведомления админу (none/all/errors)
# Уведомления отправляются в ADMIN_CHAT_ID (если задан)
SZWEGO_ADMIN_NOTIFY_MODE=errors
```

**Генерация ключа шифрования:**

```bash
python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
```

### 3. Установка зависимостей

**Windows 11:**
```powershell
pip install -r requirements.txt
playwright install chromium
```

**Ubuntu Server:**
```bash
# Системные зависимости
sudo apt-get update
sudo apt-get install -y \
    libnss3 libnspr4 libatk1.0-0 libatk-bridge2.0-0 \
    libcups2 libdrm2 libdbus-1-3 libxkbcommon0 \
    libxcomposite1 libxdamage1 libxfixes3 libxrandr2 \
    libgbm1 libasound2

# Python зависимости
pip install -r requirements.txt
playwright install chromium
```

### 4. Тестирование

**Windows 11:**
```powershell
# Тест интеграции (хранилище, шифрование)
python scripts\test_szwego_bot_integration.py

# Тест авторизации через Playwright (визуальный режим)
python scripts\test_szwego_login_playwright.py
```

**Ubuntu Server:**
```bash
# Тест интеграции
python scripts/test_szwego_bot_integration.py

# Тест авторизации через Playwright (headless)
python scripts/test_szwego_login_playwright.py
```

Подробнее см. [`docs/TESTING_SZWEGO_AUTH.md`](TESTING_SZWEGO_AUTH.md).

## Проверка работоспособности

После развёртывания проверьте:

1. **Миграция применена:**
   ```sql
   SELECT column_name FROM information_schema.columns 
   WHERE table_name = 'szwego_auth';
   ```
   Должны присутствовать: `cookies_encrypted`, `user_agent_encrypted`, `last_status`, `last_status_at`.

2. **Бот запускается без ошибок:**
   ```bash
   python -m src.main
   ```

3. **Меню настроек содержит пункт "Авторизация SZWEGO":**
   - Откройте бота в Telegram
   - Перейдите в `⚙️ Настройки`
   - Должна быть кнопка `🔐 Авторизация в SZWEGO`

4. **Авторизация работает:**
   - Пройдите авторизацию через бота
   - Проверьте, что cookies сохраняются в БД
   - Запросите товар SZWEGO — должен использоваться сохранённый session

## Откат миграции (если необходимо)

```bash
alembic downgrade -1
```

Это удалит добавленные поля из таблицы `szwego_auth`.

## Безопасность

- **Никогда не коммитьте `.env` файл** с реальными credentials
- **Регулярно ротируйте ключ шифрования** `SZWEGO_AUTH_ENCRYPTION_KEY` (при смене ключа старые данные станут недоступны)
- **Ограничьте доступ к БД** — используйте сильные пароли и ограничьте сетевой доступ
- **Мониторьте логи** на наличие ошибок авторизации
