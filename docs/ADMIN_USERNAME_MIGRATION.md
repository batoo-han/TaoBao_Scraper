# Миграция: добавление admin_username

> **Дата:** 2025-11-12

---

## 📋 Описание

Добавлено отдельное поле `admin_username` для входа в админ-панель вместо использования Telegram username.

---

## 🔄 Шаги миграции

### 1. Запустите миграцию базы данных

**Вариант 1: Через скрипт миграции (рекомендуется)**

```bash
python scripts/migrate_admin_username.py
```

Скрипт:
- Проверит наличие колонки `admin_username`
- Добавит колонку, если её нет
- Предложит установить имена пользователей для существующих админов

**Вариант 2: Через Alembic**

```bash
# Применить миграцию
alembic upgrade head
```

**Вариант 3: Вручную через SQL**

```sql
-- Добавить колонку
ALTER TABLE admin_users ADD COLUMN admin_username VARCHAR(64);

-- Создать уникальный индекс
CREATE UNIQUE INDEX uq_admin_username ON admin_users(admin_username);

-- Установить временные имена для существующих записей
UPDATE admin_users 
SET admin_username = 'admin_' || user_id::text 
WHERE admin_username IS NULL;
```

### 2. Обновите существующих админов

Если у админов установлены временные имена (например, `admin_2`), обновите их:

```bash
python scripts/update_admin_username.py
```

Или используйте скрипт миграции, который предложит обновить имена.

### 3. Создайте новых админов

При создании нового админа через `scripts/create_admin.py` будет запрашиваться имя пользователя:

```bash
python scripts/create_admin.py
```

---

## ✅ Проверка

После миграции проверьте:

1. Все админы имеют `admin_username`:
```sql
SELECT id, user_id, admin_username FROM admin_users;
```

2. Вход в админ-панель работает с новым именем пользователя.

---

## 🔙 Откат (если нужно)

Если нужно откатить изменения:

```sql
-- Удалить индекс
DROP INDEX IF EXISTS uq_admin_username;

-- Удалить колонку
ALTER TABLE admin_users DROP COLUMN admin_username;
```

Или через Alembic:
```bash
alembic downgrade -1
```

---

**Примечание:** После миграции старые админы должны будут использовать новое имя пользователя для входа, а не Telegram username.

