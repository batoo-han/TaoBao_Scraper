# 🧮 Сводка цен по SKU

## Задача

- собрать **все цены из `skus`** (поле `sale_price`) и показать их пользователю.
- сократить дублирующиеся варианты: для одинаковых цен приводим описания к одной обобщённой формулировке через LLM.
- если цена единственная, вывод остаётся прежним — одна строка `💰 Цена: X ¥ + доставка`.

## Алгоритм

1. **Фильтрация SKU**
   - берём только элементы, где:
     - есть `sale_price`;
     - `stock` > 0 (если поле присутствует);
     - значение `sale_price` успешно приводится к `float`.

2. **Отбор комбинаций `цена + props_names`**
   - берём исходную строку `props_names` (например: `Цвет: Красный; Версия: Заводной`).
   - создаём ключ `(округлённая цена, текст props_names)` и **сразу убираем дубликаты** ещё до перевода.
   - если у SKU нет `props_names`, используем заголовок товара как описание варианта.

3. **Перевод и очистка описаний**
   - переводим каждую уникальную строку `props_names` целиком через Yandex.Translate.
   - убираем служебные префиксы: «Классификация цветов», «Цветовая гамма», «Цветовая классификация».
   - заменяем `;` на запятые, сжимаем пробелы, после дефиса ставим строчные буквы (`ретривера-перезаряжаемая`).
   - если перевод пустой, используем оригинал.

4. **Обобщение через LLM**
   - группируем описания по цене.
   - если в группе один вариант — выводим как есть.
   - если вариантов несколько, отправляем список в YandexGPT с просьбой сделать одну нейтральную формулировку.
   - после ответа проверяем совместимость: каждое исходное описание должно делить хотя бы одно ключевое слово с итоговой фразой.
   - при несовместимости повторяем запрос с подсказками (ключевые слова), затем при необходимости возвращаемся к исходному описанию.

5. **Форматирование цены**
   - по умолчанию: `"{value} ¥"`, где `value` — аккуратно отформатированное число без лишних нулей.
   - если пользователь выбрал рубли и передан курс → считаем `price * rate`, округляем до 10 ₽ и добавляем символ ₽.

6. **Вывод**
   - если уникальная цена одна → показываем старую строку `💰 Цена: ...`.
   - если цен несколько:
     ```
     💰 Цены:
     - свитер (несколько расцветок) — 118 ¥ + доставка
     - свитер розовый — 130 ¥ + доставка
     - шарф большой (несколько расцветок) — 85 ¥ + доставка
     - шарф малый — 55 ¥ + доставка
     ```

## Отладка

В `DEBUG_MODE=True` удобно проверять:

- `[scraper] Максимальная цена из skus: ...` — как и раньше.
- можно временно включить вывод `print(await self._build_price_summary_lines(...))`, чтобы увидеть промежуточные строки до отправки в пост.

## Пример

Исходные SKU:

| props_names                       | sale_price | stock |
|----------------------------------|------------|-------|
| `Тип:Свитер;Цвет:Красный`        | 1000       | 24    |
| `Тип:Свитер;Цвет:Синий`          | 1000       | 18    |
| `Тип:Свитер;Цвет:Розовый`        | 1100       | 6     |
| `Тип:Шарф;Размер:Большой;Цвет:Красный` | 700 | 15    |
| `Тип:Шарф;Размер:Большой;Цвет:Синий`   | 700 | 11    |
| `Тип:Шарф;Размер:Малый`          | 500        | 40    |

Результат в посте:

```
💰 Цены:
- свитер — 1000 ¥ + доставка
- свитер розовый — 1100 ¥ + доставка
- шарф большой — 700 ¥ + доставка
- шарф малый — 500 ¥ + доставка
```

Таким образом пользователь сразу видит актуальные цены и основные различия между вариантами, а цветовые/служебные метки не засоряют вывод.
