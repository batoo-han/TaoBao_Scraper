# 📷 Распознавание текста на изображениях

## Обзор

Начиная с ветки `feature/image-text-recognition` бот анализирует все фотографии товара **до фильтрации по размерам и весу**:

1. **Загрузка изображений**  
   Используются SKU-фото и детальные изображения из `detail_html`. Для Taobao/Tmall выполняется определение размеров, но сами изображения не отфильтрованы.

2. **Rаспознавание (OCR) через Yandex Vision**  
   Клиент `YandexVisionClient` (`src/api/yandex_vision.py`) отправляет байтовый контент в `vision/v1/batchAnalyze`.  
   Обрабатываются блоки `TEXT` и `TABLE`, поддерживается запасная обработка `fullText`.
   Языки распознавания ограничены русским и английским (`languageCodes = ["ru", "en"]`), что уменьшает шум от китайских надписей.

3. **Перевод текста**  
   Все найденные фрагменты и ячейки таблиц переводятся на целевой язык (по умолчанию `ru`) через уже существующий `YandexTranslateClient`.

4. **Суммаризация через LLM**  
   Собранный текст и таблицы передаются в **выделенный LLM-провайдер** (настраивается отдельно от основного провайдера для генерации описаний) с отдельным промптом. Возвращается JSON с материалами, составом, цветами, особенностями, назначением и описанием таблиц. Провайдер, модель, температура и максимальное количество токенов настраиваются независимо в админ-панели.

5. **Визуализация таблиц**  
   Таблицы строятся заново в виде PNG (`render_table_image`), сохраняются в `IMAGE_TEXT_OUTPUT_DIR` и добавляются в список медиа для Telegram (с префиксом `local::`).

6. **Интеграция с основным описанием**  
   Набор данных передается в `Scraper`:
   - агрегированный текст попадает в `compact_data['image_text']`;
   - инсайты вливаются в `main_characteristics`;
   - таблицы передаются в `compact_data['image_tables']`, а их визуализации — в выдачу бота.

## Основные файлы

| Файл | Назначение |
|------|------------|
| `src/api/yandex_vision.py` | Легковесный клиент Yandex Vision |
| `src/services/image_analysis/service.py` | Оркестрация OCR → перевод → LLM → рендер |
| `src/services/image_analysis/renderer.py` | Построение PNG с таблицами |
| `src/core/scraper.py` | Интеграция в общий pipeline, слияние данных |
| `src/bot/handlers.py` | Поддержка локальных изображений в выдаче |

## Конфигурация

| Ключ | Описание | Требует перезапуск |
|------|----------|-------------------|
| `ENABLE_IMAGE_TEXT_ANALYSIS` | Включить/выключить весь модуль анализа изображений | Нет |
| `YANDEX_VISION_API_KEY` | API ключ Yandex Vision OCR | Да |
| `YANDEX_VISION_FOLDER_ID` | Отдельный ID каталога в Yandex Cloud для Yandex Vision (если ключ относится к другому каталогу) | Да |
| `YANDEX_VISION_MODEL` | Модель Vision (`ocr`, `inline-text`) | Да |
| `IMAGE_TEXT_TRANSLATE_LANGUAGE` | Целевой язык перевода распознанного текста (`ru`, `en`) | Нет |
| `IMAGE_TEXT_LLM_VENDOR` | Провайдер LLM для анализа текста с изображений (`yandex`, `openai`, `proxiapi`) | Нет |
| `IMAGE_TEXT_LLM_MODEL` | Модель LLM для суммаризации текста с изображений | Нет |
| `IMAGE_TEXT_LLM_PROMPT` | Промпт для анализа распознанного текста и таблиц | Нет |
| `IMAGE_TEXT_LLM_TEMPERATURE` | Температура генерации для LLM анализа (0.0-1.0) | Нет |
| `IMAGE_TEXT_LLM_MAX_TOKENS` | Максимальное количество токенов в ответе LLM (1-4000) | Нет |
| `IMAGE_TEXT_OUTPUT_DIR` | Каталог для сохранения визуализированных таблиц (по умолчанию `data/image_analysis`) | Нет |

**Примечания:**
- Настройки LLM для анализа изображений **независимы** от глобальных настроек LLM для генерации описаний товаров
- Настройки доступны в админке: вкладка **Настройки → Изображения**
- Настройки с пометкой "Требует перезапуск" будут применены только после перезапуска сервисов

## Работа в Telegram

Телеграм теперь принимает как удалённые URL, так и локальные файлы:

- Для локальных файлов формируется путь `local::<абсолютный-путь>`.
- `handlers.py` отправляет такие медиа как `BufferedInputFile`, а в случае ошибки повторно читает файл напрямую.

## Пример респонса LLM

```json
{
  "materials": ["Хлопок 100%"],
  "composition": "Верх — хлопок, подклад — полиэстер",
  "colors": ["белый", "серый"],
  "usage": ["повседневная носка", "офис"],
  "special_features": ["машинная стирка при 30°C"],
  "notes": "Размерная сетка на изображении подтверждает замеры в сантиметрах.",
  "tables": [
    {"index": 1, "title": "Размерная сетка", "summary": "S-XL, рост 160-175 см"}
  ]
}
```

## Потенциальные ограничения

- Ограничение распознанного текста определяется возможностями Yandex Vision и лимитом 16 МБ на изображение.
- Перевод выполняется пофрагментно; при больших объёмах текстов возможна задержка.
- Поддержка других LLM-провайдеров для анализа изображений: Yandex (реализовано), OpenAI и ProxiAPI (поддержка через общую архитектуру LLM).

## Интеграция с админ-панелью

Все настройки анализа изображений доступны в веб-интерфейсе админ-панели:
- Раздел **Настройки → Изображения**
- Группировка настроек по назначению
- Визуальные элементы управления (чекбоксы, селекты, ползунки для температуры и токенов)
- Автоматическое определение необходимости перезапуска
- Уведомления об успешном сохранении или необходимости перезапуска

