# Mimi App — быстрые настройки бота

Документ фиксирует архитектуру мини-приложения Telegram, бизнес-логику и инструкцию по эксплуатации.

## Архитектура

- **Сервер Mimi App** — модуль `src/webapp/server.py` на базе `aiohttp`. Он запускается параллельно с Telegram-ботом и обслуживает HTTPS/HTTP-запросы от Telegram Mini App.
- **Статика** — `src/webapp/static/` содержит адаптивную HTML/CSS/JS-страницу, взаимодействующую с Telegram WebApp API и REST API сервера.
- **Авторизация** — каждый запрос подписывается `initData` от Telegram. Подпись проверяется по Bot Token (см. `src/webapp/auth.py`). Учитывается таймаут 10 минут.
- **Хранилища**:  
  - персональные настройки пользователей (`UserSettingsService`) в `data/user_settings.json`;  
  - глобальные административные параметры (`AdminSettingsService`) в `data/admin_settings.json` с горячим обновлением объекта `settings` и сбросом LLM-кэшей.
- **Интеграция** — кнопка WebApp добавлена в клавиатуру настроек Telegram (`handlers.py`). Публичный URL берётся из `MINI_APP_URL`, чтобы Telegram знал, что открывать.
- **Деплой** — сервер Mimi App слушает `MINI_APP_HOST:MINI_APP_PORT` (по умолчанию `0.0.0.0:8081`) и отдаётся наружу через reverse-proxy/HTTPS. Docker-компоуз публикует порт контейнера и использует существующий `.env`.

## Бизнес-логика

- **Пользователь** (любой чат): может изменять подпись, валюту (CNY/RUB) и курс для расчёта. UI предотвращает ввод некорректных значений, а сервер дополнительно валидирует длины/диапазоны.
- **Администратор** (`ADMIN_CHAT_ID`): видит дополнительный блок Mimi App с возможностью:
  - выбрать основного провайдера LLM (Yandex/OpenAI), задать модели обеих платформ;
  - указать отдельный провайдер/модель для перевода SKU и принудительно включить legacy-переводчик;
  - включать/отключать ключевые опции бота (`CONVERT_CURRENCY`, `TMAPI_NOTIFY_439`, `DEBUG_MODE`, `MOCK_MODE`).
- **Валидация** — все административные действия доступны только после проверки подписи Telegram + проверки ID администратора.
- **Персистентность** — каждое изменение мгновенно сохраняется на диск, обновляет объект `settings` и обнуляет кэши клиентов LLM, поэтому бот продолжает работу без перезапуска.

## План реализации (фактически выполнен)

1. Подготовка вспомогательных сервисов (`AdminSettingsService`, синглтон `UserSettingsService`).
2. Добавление REST-сервиса `MiniAppServer` и утилиты валидации `validate_init_data`.
3. Создание статического фронтенда на чистом JS + Telegram WebApp SDK.
4. Добавление кнопки Mimi App в меню Telegram и новых переменных окружения.
5. Документация + обновление Docker/requirements.

## Руководство пользователя

1. **Настроить окружение** в `.env`:

   ```dotenv
   MINI_APP_URL=https://bot.example.com/mini-app   # URL, на который смотрит Telegram
   MINI_APP_HOST=0.0.0.0                           # локальный хост сервера
   MINI_APP_PORT=8081                              # порт, который нужно пробросить наружу
   MINI_APP_BASE_PATH=/mini-app                    # URL-путь (можно поменять при необходимости)
   ```

   После изменения переменных перезапустите контейнер/процесс.

2. **Запустить бота**. Mimi App стартует автоматически и доступна по `http(s)://HOST:PORT/mini-app`. Для работы с Telegram требуется публичный HTTPS-домен (можно использовать nginx/caddy + обратный прокси).
3. **Открыть Mimi App** в Telegram: зайдите в настройки бота и нажмите кнопку `🧩 Mimi App`.
4. **Пользовательский сценарий**:
   - обновите подпись, валюту и курс;
   - сохраните изменения — данные применятся немедленно.
5. **Админ-сценарий** (ADMIN_CHAT_ID):
   - в блоке «Провайдеры» выберите нужные модели и сохраните;
   - в блоке «Опции» переключите требуемые флаги и сохраните;
   - изменения вступают в силу без рестартов.
6. **Отладка**: проверяйте health-эндпоинт `GET /mini-app/health`. Ошибки логируются в общий лог бота.

## Дополнительные замечания

- Все секреты (Bot Token, ключи API) остаются в `.env`. Mimi App использует только пересекающиеся настройки.
- Мини-приложение не хранит собственных cookies или локальной БД — достаточно резервного копирования `data/*.json`.
- Для продакшена рекомендуется ограничить доступ к API Mimi App только через Telegram (проверяется подпись) и, при необходимости, через внешнюю аутентификацию/брандмауэр.

