# 📋 Экспорт Cookies из Chrome

Скрипт `export_chrome_cookies.py` позволяет быстро извлечь все cookies для `https://mobile.yangkeduo.com/` из Chrome и сохранить их в формате `pdd_cookies.json`.

## 🚀 Быстрый старт

### 1. Установка зависимостей

```bash
pip install -r scripts/requirements-export.txt
```

Или вручную:

```bash
pip install selenium webdriver-manager
```

### 2. Запуск скрипта

```bash
python scripts/export_chrome_cookies.py
```

### 3. Что происходит

1. Скрипт запускает Chrome (автоматически устанавливает ChromeDriver)
2. Открывает `https://mobile.yangkeduo.com/`
3. Извлекает все cookies для этого домена
4. Конвертирует их в формат `pdd_cookies.json`
5. Сохраняет в файл `scripts/chrome_cookies.json`

### 4. Использование результата

После выполнения скрипта:

**Вариант 1: Скопировать содержимое**
```bash
# Скопируйте содержимое chrome_cookies.json в src/pdd_cookies.json
cp scripts/chrome_cookies.json src/pdd_cookies.json
```

**Вариант 2: Использовать напрямую**
Измените в `.env`:
```env
PDD_COOKIES_FILE=scripts/chrome_cookies.json
```

## ⚠️ Важные замечания

1. **Chrome должен быть закрыт** - закройте все окна Chrome перед запуском скрипта
2. **Авторизация** - убедитесь, что вы уже авторизованы на `mobile.yangkeduo.com` в вашем основном Chrome
3. **Профиль** - по умолчанию скрипт использует новый профиль Chrome. Если нужно использовать существующий профиль, раскомментируйте строки в скрипте:
   ```python
   chrome_options.add_argument("--user-data-dir=C:/Users/YourUser/AppData/Local/Google/Chrome/User Data")
   chrome_options.add_argument("--profile-directory=Default")
   ```

## 🔍 Что делает скрипт

- Фильтрует cookies по доменам:
  - `mobile.yangkeduo.com`
  - `yangkeduo.com`
  - `pinduoduo.com`
  - `szwego.com`
- Сохраняет User-Agent браузера
- Сохраняет дату и время экспорта
- Конвертирует формат cookies из Selenium в формат `pdd_cookies.json`

## ❌ Возможные ошибки

### "ChromeDriver не найден"
- Установите `webdriver-manager`: `pip install webdriver-manager`
- Скрипт автоматически скачает ChromeDriver при первом запуске

### "Chrome не запускается"
- Убедитесь, что Chrome установлен
- Закройте все окна Chrome
- Попробуйте запустить скрипт от имени администратора

### "Cookies не найдены"
- Откройте `https://mobile.yangkeduo.com/` в Chrome вручную
- Авторизуйтесь на сайте
- Запустите скрипт снова

## 📝 Формат выходного файла

Файл `chrome_cookies.json` имеет такой же формат, как `pdd_cookies.json`:

```json
{
  "cookies": [
    {
      "name": "cookie_name",
      "value": "cookie_value",
      "domain": ".yangkeduo.com",
      "path": "/",
      "expires": "2026-12-31T23:59:59Z",
      "httpOnly": true,
      "secure": true,
      "sameSite": "None"
    }
  ],
  "user_agent": "Mozilla/5.0...",
  "saved_at": "2025-01-15 12:00:00",
  "url": "https://mobile.yangkeduo.com/"
}
```

